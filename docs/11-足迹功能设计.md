# 11 足迹功能设计（Footprint Records）

> 2026-09-20 · 状态：设计已确认，待实现计划
> 范围：小程序新增「足迹」tab + 后端 `footprint-records` 模块。与现有运动轨迹点亮地图（packageFootprint / `GET /stats/footprint`）**完全独立、互不引用**。

## 1. 需求与决策记录

用户可创建"自己去过的地方"的足迹记录，字段：时间（年月日）、地点、人物、标题、描述、相关图片（最多 3 张，本地先压缩）。地点支持搜索与地图选点；因腾讯关键词搜索有额度限制，**主选点方式用 `wx.chooseLocation`（免费，自带搜索框+地图点选）**，另提供自建地图选点页作为补充入口。足迹地图用原生地图自带 marker 聚合簇展示密集区域，稀疏区域直接展示点，点击点弹出详情弹窗。

已确认决策：

| 决策点 | 结论 |
|---|---|
| 与点亮地图关系 | 完全独立，点亮地图仍从「我的」进入 |
| 可见性 | 仅自己可见，全部接口 `authenticate`，查询强制 `userId` 过滤 |
| 选点方式 | `wx.chooseLocation` 为主 + 自建地图选点页（关键词搜索走后端代理腾讯 place API） |
| 数据归属 | 独立 collection，不复用 activity marker（语义与生命周期不同） |

## 2. 数据结构

### 2.1 `FootprintRecord`（`src/models/footprint-record.model.ts`，collection `footprintrecords`）

```ts
const locationSchema = new Schema({
  name:     { type: String, default: '' },   // 地点名（POI 名或自填）
  address:  { type: String, default: '' },   // 详细地址
  province: { type: String, default: '' },   // 服务端由 region.ts locateRegion 离线补全
  city:     { type: String, default: '' },
  adcode:   { type: String, default: '' },
  latitude:  { type: Number, required: true },
  longitude: { type: Number, required: true },
}, { _id: false });

const footprintRecordSchema = new Schema({
  userId:      { type: Schema.Types.ObjectId, ref: 'User', required: true },
  visitDate:   { type: String, required: true },   // 'YYYY-MM-DD'，字符串免时区问题、可直接排序
  title:       { type: String, required: true, trim: true },
  people:      { type: [String], default: [] },
  description: { type: String, default: '' },
  location:    { type: locationSchema, required: true },
  photos:      { type: [String], default: [] },    // 裸 OSS URL，读时签名
  category:    { type: String, default: '' },      // 2026-09-22 新增：分类 key，'' = 未分类；取值在 zod 层闸门
}, { timestamps: true, versionKey: false });

footprintRecordSchema.index({ userId: 1, visitDate: -1 });
```

Zod 校验（`src/utils/validators.ts` 新增）：

- `visitDate`：`/^\d{4}-\d{2}-\d{2}$/`，不校验是否未来日期（允许记"计划去"）。
- `title` 1–50 字符；`description` ≤ 500；`people` 最多 10 个、每个 ≤ 20 字符；`photos` 最多 3 个 URL；`location.name` ≤ 100、`address` ≤ 200。
- 列表分页复用现有 `page/pageSize`（默认 20，max 100）。
- **分类是代码内枚举，不是后台可配数据**：`src/utils/footprint-category.ts` 持 key+中文名，小程序 `config.FOOTPRINT_CATEGORIES` 多一份 key→图标，webAdmin `src/utils/footprintCategory.ts` 一份 key→中文名。理由：地图 marker 的 `iconPath` 只吃打进小程序包的静态 PNG，做成后台 CRUD 就得配套图片上传与包体策略，成本不对等。9 类：景区/山峰/公园绿地/古迹寺庙/博物馆展馆/商圈街区/餐饮咖啡/露营户外/其他，外加 `''` 未分类。非法值的 400 报文带实际值与可选清单（`分类不合法：'temple' 不在可选范围内（可选：景区/…/未分类）`）。

### 2.2 图片存储

沿用现有 OSS 直传体系：`POST /oss/credential` 签发 `dir` 白名单正则已兼容 `footprints`（`/^[a-z0-9-]+$/`），key 前缀天然按用户隔离 `.../users/{userId}/footprints/`。DB 存 `cleanUrl` 后的裸 URL，响应时 `getSignedUrl(url, 86400)`。

## 3. 后端接口

路由文件 `src/routes/footprint-record.routes.ts`，注册前缀 `${apiPrefix}/footprint-records`（`src/app.ts`），服务层 `src/services/footprint-record.ts`，越权处理抄 `findOwnedActivity`（`findOne({_id, userId})` 找不到即 404）。响应包裹沿用 `{success, code, message, data}`。

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/footprint-records` | 创建。服务端补全 province/city/adcode（离线 `locateRegion`，不耗额度）。防刷：每用户 1h 最多 30 条，超限返回与 activities `assertCanCreate` 相同的状态码与文案风格 |
| GET | `/footprint-records` | 分页列表，`?page&pageSize&keyword`（keyword 对 title/description/location.name/address/**province/city**/people/visitDate 做不区分大小写正则匹配，省市是 2026-09-22 补的），`visitDate` 倒序、同日按 `createdAt` 倒序；photos 签名 |
| GET | `/footprint-records/geo` | 地图专用轻量字段：`{id, title, visitDate, category, province, city, latitude, longitude, coverPhoto, coverPhotoThumb}`（coverPhoto=photos[0] 签名），不分页（私有数据、量级可控），按 visitDate 倒序。**2026-09-22 起支持过滤**：`?keyword&province&year&category`（四者可叠加；`year` 必须 4 位数字，`category` 走同一枚闸门，非法 400）。不带任何参数时行为与历史完全一致（老客户端不受影响）。下发 `province/city/category` 是给地图页筛选弹窗**在前端算候选项**用的，不再单开聚合接口 |
| GET | `/footprint-records/stats` | **2026-09-21 新增**。统计页数据：`?from&to`（均为 `YYYY-MM-DD`，含 from 不含 to，省略即全部；服务端校验格式与 `from < to`，非法 400）。一次 `$group` 按 `{province, city}` 聚合后在 Node 侧汇总，返回 `{total, provinceCount, cityCount, provinces:[{name, count, cities:[{name, count}]}]}`（provinces 按 count 降序、同数按名称；**2026-09-22 起每省带城市明细**，城市同样计数倒序，省为空或市为空的脏数据不进 `cities` → 前端据此判定「不可展开」，`provinceCount/cityCount` 口径不变）。`$match` 里的 `userId` 必须 `new Types.ObjectId(userId)`——aggregate 不像 find 那样自动转型 |
| GET | `/footprint-records/calendar` | **2026-09-22 新增**。日历态数据：无参数、全量、不分页（同 `/geo` 先例）。一次 `$group by visitDate` 同时累加 `$size photos` 并 `$addToSet location.name`，返回 `{total, placeCount, photoCount, days:[{date, count}]}`（days 按 date 倒序）。`placeCount` 是 **distinct `location.name`**（"个地方"按地点名，空名不计；城市数在 `/stats` 不重复口径）。前端换月只在本地过滤 `days`，不再发请求 |
| GET | `/footprint-records/:id` | 详情，photos 签名 |
| PUT | `/footprint-records/:id` | 整体更新（含 photos 集合替换；差集部分服务端 `deleteOssObjects` 清理旧图） |
| DELETE | `/footprint-records/:id` | 删除记录并清理其全部 OSS 图片 |

选点辅助（放现有 geo 模块，`src/routes/geo.routes.ts`）：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/geo/search?keyword&latitude&longitude` | **新增**。后端代理腾讯 `place/v1/search`（region 优先按 keyword，location 就近）。唯一耗额度的接口：进程内缓存（keyword 归一化 key，TTL 1h，仿 `geo.ts` 现有缓存）+ 每用户限流（如 10 次/分钟）。返回精简 `{id, name, address, latitude, longitude}` 列表，max 20 |
| GET | `/geo/reverse` | **已存在**，自建选点页点选地图后回填地址，直接复用 |

错误语义：401 未登录；404 不存在/非本人；400 校验失败（ZodError 走现有 error-handler）；限流 429。**非法 id 一律 404（2026-09-22 全站收口）**：客户端 id 形态不合法时，畸形串会直接进 mongoose 的 `_id`（或任何 ObjectId 路径）查询并抛 CastError，而现有 error-handler 不认识它 → 落 500 分支并把「Cast to ObjectId failed for value … for model …」这类内部文案透给客户端（dev 环境）。收口方式＝新增 **`src/utils/object-id.ts`**（`isObjectIdLike` / `assertObjectIdLike(value, 资源文案)`，只认 24 位 hex 串与 ObjectId 实例；比 `Types.ObjectId.isValid` 严——本机 mongoose 9.9.2 实测它还放过 12 字节 Buffer），接入点：`findOwnedActivity` + 五个自带 findOne 的活动入口（appendPoints/addMarker/finishActivity/cancelActivity/getActivityDetailView）、`findOwnedRecord`（足迹 GET/PUT/DELETE 共用）、topic 三处（原内联 `OBJECT_ID_RE` 迁过来）、admin 六个按 id 查的接口（含此前**没有**闸门的 `users/:id/login-logs`、`login-stats`）。admin 的 `activities/recompute-elevation?maxId=` 游标改**直接 400**（空结果时非法 maxId 会回落进 `new Types.ObjectId()` 抛 500）。回归：API **186 pass / 0 fail**（新增 `test/object-id.test.ts` 形态直测、活动 11 条路由非法 id 全 404、admin 登录两接口 404 + maxId 400），typecheck EXIT=0。

## 4. 小程序结构

### 4.1 tabBar 与页面归属

- `app.json`：tabBar 增第 4 项「足迹」→ `pages/footprints/footprints`（**tabBar 页必须在主包**）。图标 `assets/icons/tab-footprint-{gray,blue}.png`，风格对齐现有 tab 图标。
- **2026-09-21 现状：不再新增分包**——新增/编辑表单改为主包组件 `components/footprint-form`（半屏弹层，见 §4.3），tab 页内就地打开、不跳页面。历史：先建分包 `packageFootRecords`（`record-edit` 新增/编辑页 + `map-picker` 自建地图选点页，包名多一个 s 以避开现有 `packageFootprint` 点亮地图）；当日先移除 `map-picker`（commit `9777849`）、再随表单改组件移除整个分包（commit `d723c69`）。
- **2026-09-21 第二轮（本文件 §4.2 末「全屏改版」条）**：主包新增独立列表页 `pages/footprint-list/footprint-list`（列表形态从 tab 页迁出，tab 页只留全屏地图）；现有分包 `packageFootprint` 新增 `pages/footprint-stats/footprint-stats` 足迹统计页（放分包以复用 `components/ec-canvas` 点亮地图，分包页面按既有方式取 api = `getApp().globalData.api`）。两页均由地图页左上浮层入口 navigateTo 进入。
- 点亮地图入口维持 `pages/my/my.js` 现状不动。

命名注意：tab 页路径用 `footprints`，接口/模型用 `footprint-record(s)`，均避开已占用的 `footprint`（点亮地图、`user.footprintCache`、`/stats/footprint`）。

### 4.2 tab 主页 `pages/footprints/footprints`

> **2026-09-21 全屏改版（本节末「全屏改版」条）**：顶部 segmented control 与页内列表形态**已整体移除**——地图改为全屏独占 tab 页，列表迁至独立页 `pages/footprint-list/footprint-list`，另加分包统计页 `packageFootprint/pages/footprint-stats/footprint-stats`；两个入口以左上浮层按钮形式放在地图上。以下原描述与其实现验证记录均为改版前形态，留档不撤销。

- 顶部 segmented control：**地图 ⇄ 列表** 双形态，默认地图；右下角 FAB「＋」→ navigateTo 编辑页。
- **地图形态**：原生 `<map>`，数据来自 `GET /footprint-records/geo`。
  - marker 聚合（Task 5 修复轮定稿，官方 `component/map`「marker 聚合」+ `MapContext` 文档 + 模拟器实测）：**【本条与其下「实测修正的设计约束」条已于 2026-09-21 取代，见本节末「簇路线变更记录」】采用原生 `MapContext.initMarkerCluster` + `MapContext.addMarkers`（地图自带聚合簇）** —— `initMarkerCluster({enableDefaultStyle:true, zoomOnClick:true, gridSize:60, minClusterSize:2})`，密集点 marker 带 `joinCluster:true` 由地图渲染自带蓝底数字气泡；点簇原生放大展开，展开后的叶 marker 与稀疏孤点走 `bindmarkertap → openPopup`。契约不变：`markerId = records 下标 + 1`、`openPopup(record)`。
    实测修正的设计约束（**同样已于 2026-09-21 取代，见本节末「簇路线变更记录」**）：① 聚合要求 marker 命令式 `addMarkers` 注入，`<map>` 不再绑 `markers` 属性；② **`joinCluster:true` 的孤点也会被画成「1」字气泡**（与「稀疏区域直接展示点」不符），故 `joinCluster` 只给本地网格（`utils/footprint-geo.js#gridCluster`）判定为多点同处的点；③ 模拟器只回 `addMarkers` 的 `success`、不回 `onComplete`，`<map>` 的 `bindload` 不触发，注入时机挂在数据 setData 回调；④ 最大缩放仍并在一起的簇（同坐标点）走**成员列表半屏**兜底（`markerClusterCreate` 回灌成员表 + `markerClusterClick`，模拟器未派发过这两个事件，链路以真机回归为准）。
    历史结论留档：设计初稿写的声明式 `<map clusters>` / `bindclusterclick` / `bindclusteritemclick` 在微信文档中不存在（按其他家 SDK 形状写的）；初版曾降级为组件内网格聚合 + 离屏 canvas 自绘气泡，修复轮实测原生可渲染后已删除该降级实现。
  - 初始视野：`include-points` 实测形状是 `Array<{latitude, longitude}>`（文档无 `include-padding`，也没有 per-point padding），**没有 padding 可调** —— 点会正好贴边被裁一半，且单点包围盒零面积会把 scale 顶到 20（与 `components/track-map` 同一个坑）。故视野改为 `utils/footprint-geo.js#fitBounds` 自行算 `center` + `scale`（内边距 40/50/120，多点 scale 封顶 15、单点固定 12），纯函数已配 `test/footprint-geo.test.js` 单测。
- **列表形态**：分页 `GET /footprint-records`，卡片=缩略图(photos[0])+标题+日期+`location.city||name`，样式基准对齐轨迹页卡片；触底加载下一页。
- **详情弹窗**（地图/列表共用，自定义 overlay 组件，半屏底部弹出）：标题、日期、人物标签、描述、≤3 图横滑（签名 URL）、按钮「编辑」「删除」（删除需二次确认）。列表侧当前分页数据可直接提供弹窗内容；地图侧点 marker 时按 id 从已拉取的 geo 数据取，缺失字段（描述/人物）时请求 `GET /:id` 补齐。
- 空态：插画+文案「还没有足迹，点这里记录第一个去过的地方」。
- 加载/错误处理遵循既有规范：切换形态不闪屏、请求带序号守卫、失败可重试。

#### 实现验证记录（2026-09-20，Task 9 收尾）

- **原生聚合路线定稿并已在模拟器实测通过**：`MapContext.initMarkerCluster({ enableDefaultStyle:true, zoomOnClick:true, gridSize:60, minClusterSize:2 })` + `addMarkers({ markers, clear:true })` 命令式注入，密集点由**地图自带蓝底数字气泡**渲染（非自绘），稀疏孤点走普通 marker + `bindmarkertap`；组件内网格聚合的降级实现已删除。证据（Task 5 **修复轮 1 定稿原生路线 → 修复轮 2 冷启动注入修复**；截图目录 `.superpowers/sdd/12-足迹功能实现计划/shots/`，报告 `task-5-report.md` Fix round 1 / Fix round 2）：`t5-fx-1-native-bubbles-6-1-1.jpg`（全点 `joinCluster` 时地图自带「6」+「1」+「1」蓝底气泡 → 原生可渲染，同时暴露孤点坑②）、`t5-fx-2-native-cluster5-fitted.jpg`（fitBounds 视野 scale 4 下的自带「5」字簇）、`t5-fx-3-native-final-cluster5-leaves.jpg`（**定稿形态**：密集 5 点 = 原生「5」气泡，稀疏北京/丽江 = 普通叶 marker 圆点）、`t5-fx2-1-coldstart-markers.jpg`（修复轮 2 真·冷启动首屏即见「5」字簇 + 全国视野）。**注**：`t5-12-map-clusters.png`／`t5-13-leaves.png`／`t5-14-popup.png` 三张属**已被取代的组件内网格聚合轮**（当时的降级实现），不作为原生路线证据。初版结论（声明式 `clusters` 属性不存在、原生不可用）已在同一文档 §4.2 改写并留档。
- **腾讯 WebService 日配额**：Task 8 验证当日 `/geo/reverse`、`/geo/search` 上游返回 `status 348`（当日额度耗尽，两接口降级为空地址/空列表）。Task 9 收尾（同日 22:40）以 mock 登录签发的 dev token 真实 curl `GET /sport-track/api/geo/search?keyword=火车站&latitude=30.2450&longitude=120.1450` → **HTTP 200 + `data:[]`**，服务端日志 `[geo] place 搜索 status= 121`（同为日配额耗尽族错误码，place 接口额度仍未恢复）。故「带真实搜索结果的选点截图」仍待次日额度恢复后补拍；按裁定只有 `status===0` 的成功响应进 1h 缓存，额度耗尽不会被毒化成整小时空结果（`test/geo-search.test.ts` 已覆盖）。
- **只剩真机能验的项（本轮无法在模拟器/本机证实）**：① 真实手指点击叶 marker 与簇气泡的热区（模拟器历史留证均为合成事件驱动，坐标 tap 对同层渲染的 map 不可靠）；② `wx.chooseLocation` / `wx.chooseMedia` 系统面板（模拟器不弹真实面板，本轮只验到回调与上传链）；③ `markerClusterCreate` / `markerClusterClick` → 簇点击/最大缩放成员列表半屏（模拟器对这两个事件零派发）；④ 未登录进 tab 的提示态——本轮按要求**不注销账号**，故**没有未登录态的截图**，只有代码路径级证据：`pages/footprints/footprints.wxml:9` 的 `wx:elif="{{error}}"` 错误分支（`{{error}}` + `.retry` → `bindtap="loadAll"`）与 `footprints.js loadAll` 的 `catch` → `setData({ error: e.message || '加载失败' })`（`:69-75`），同一分支自 Task 4 起未被后续轮次改动（Task 4 报告 concerns 2 亦记「无 token 时按 spec 落 error/retry 态，代码路径已就位但未单独截图验证」）。**更正**：Task 4 的 `task-4-tab-error.png` 拍的是「有未保存的运动」弹窗盖在空 body 上的 tab 选中态，**并未呈现未登录错误态**（该态从未被截图留证），不作为本项证据。真机未登录表现（应见提示 + 重试而非白屏）列为用户 TODO。
- **`chooseLocation` 后台接口权限（发布前置，代码侧无需改动）**：`app.json` 的 `requiredPrivateInfos` 已声明 `chooseLocation`，但正式环境必须在微信公众平台「开发 → 开发管理 → 接口权限」申请「地理位置（选择位置）」接口权限并说明用途，否则真机调用直接 `auth deny`（编辑页只弹「需要位置权限才能选点」toast）。
- **模拟器 storage 遗留键（交由用户定夺，本轮未触碰）**：Task 8 验证期间清掉了 tracker 测试残留 `pending_summary`，留下成对键中的孤儿 `pending_sync_activity = 6aabaeb57ded933fa635dd5b`。小程序侧无功能影响（`summary.js` 以 `pending_summary` 为唯一闸门，下一次正常结束运动也会覆盖它），但服务端可能仍挂着一条 `in_progress` 活动（依赖 24h 惰性清理收口）；`pending_sync_activity`/`pending_summary` 均不属于足迹功能，足迹回归全程未读写。

#### 簇路线变更记录（2026-09-21，原生聚合定稿被取代）

- 3104c7c 给 `initMarkerCluster` 追加的簇样式参数（`size/color/bgColor/borderWidth/borderColor`）**实测无任何效果**（改前改后模拟器截图的簇气泡尺寸/配色一致，判定运行时对旧基础库静默忽略），原生默认数字气泡过大、不符合「小圆圈」诉求。经用户裁定聚合改回**本地网格聚合 + 离屏 canvas 自绘小圆气泡**（`pages/footprints/footprints.js#buildClusterIcon`：直径 24 CSS px、2x 出图，蓝底白字 2px 白描边，尺寸/颜色常量在文件顶部可调，图标按 count 缓存）；`<map>` 恢复声明式 `markers="{{markers}}"` 绑定，原生聚合机械（`initMarkerCluster`/`addMarkers`/`joinCluster`/`_pendingNative` 挂载闸门注入/`markerClusterCreate`/`markerClusterClick`/`onNativeClusterClick`/`trackMapNode`）整体删除——声明式属性在 map 节点重建（loading 闸门、地图⇄列表切换）时自动带上最新 markers，此前「marker 只能投给已存在节点」的注入时序问题不复存在。交互契约不变：点簇向簇心本地放大 +2 级（封顶 scale 20，`regionchange` 静默窗口内不回读重建）、到顶仍同格（同坐标点）的点簇 → 既有成员半屏列表、叶 marker → `openPopup`。
- **上面「只剩真机能验的项」第③项（`markerClusterCreate`/`markerClusterClick` → 簇点击/最大缩放成员列表半屏）作废**：聚合分桶、点簇展开与封顶成员列表全部回到本页可控代码路径，不再依赖原生聚合事件；该段与「原生聚合路线定稿并已在模拟器实测通过」条为其当时有效记录，留档不撤销。真机回归改看：自绘小圆气泡渲染（24px 蓝底白字白描边）、点簇逐级放大、放大到顶点簇弹成员半屏、稀疏点叶 marker 开详情。
- 自绘路线的页级回归已入仓：`test/footprints-page.test.js`（`npm test` 直跑，无第三方依赖，stub `wx`/`Page`/`getApp` 后驱动真实 `footprints.js`）覆盖分桶守恒、逐级展开、封顶成员半屏、叶弹窗快路径之外，另锁三条交互回归：手势缩放后 `data.scale` 回写（缩小再点簇仍应放大，而非误弹成员表）、并发构建的过期提交被丢弃、同 count 簇共用一次 canvas 出图。退化形态（canvas 不可用 → ALWAYS callout 数字）的点击由 `<map bindcallouttap="onMarkerTap">` 兜住，与 `bindmarkertap` 同处理函数。
- **2026-09-21 复审跟进（polish 轮，`onRegionChange` 相机回写两处收口）**：① 中心要异步问 `getCenterLocation`，问回来之前可能又并进一次手势，故相机回写配套**写序令牌** `_camWriteSeq`——只有最后取号那次有权写回，过期回读（含「回读在途期间 `expandCluster` 已程序化挪走相机」这种令牌仍最新的情形，靠静默窗口复校挡）整次丢弃；`commit` 里那次 `<0.5` 复校只挡「层级几乎没变」的提交，判不了次序。**回写需配套当前中心**这一点是 `components/track-map`（`applyOverviewScale`）的既有教训：`<map>` 的 scale 属性一改就把相机重置到绑定的 center 上。② 中心问不到（端上无 `getCenterLocation`/回读 `fail`）不再整条放弃，降级为「只同步 `_gridZoom` + `buildMarkers({fit:false})` 重建分桶、一个相机属性都不 setData」——`fit:false` 的 patch 为空，视野原地不动，也就没有弹回可言。③ **已知限制**：pan-only 手势（层级几乎不变）按闸门直接放行、**不写 center**，所以地图 ⇄ 列表往返把 `<map>` 重建时视野会回到「上一次同步过的中心」而非用户最后平移到的位置；要修得让纯平移也回读一次中心（多一次异步 MapContext 调用），本轮不做。回归见 `test/footprints-page.test.js` P8（8a 过期回读不写回 / 8b 静默窗口内不写回 / 8c 问不到中心只同步层级）。

#### 全屏改版（2026-09-21，用户定稿：地图全屏 + 列表/统计独立页）

**动机**：地图上方压着 segmented control、地图只占视口下半，观感局促；用户要求「地图充满全屏、切换模式放到地图上面、列表以新页面展示」，并新增足迹统计页。

- **tab 主页只剩地图**：`.page{position:relative;height:100vh;overflow:hidden}` + `.fp-map{width:100%;height:100%}`；`mapViewport()` 去掉原先减 112rpx（分段条高度）的补偿——`fitBounds` 的像素分格与视野现在同源，全屏后取 `windowHeight` 原值。
- **浮层入口**（左上 `.map-entries`：白底 pill「列表」「统计」（各带 32rpx 图标，列表用 `lucide-text-align-justify` 与轨迹页列表模式同款、统计用 `lucide-chart`）→ `navigateTo` 列表页/统计页；右上 `.map-refresh` 刷新胶囊：lucide `refresh-cw` 图标（30rpx，与入口图标同族，本地 PNG 资产）+「刷新」文案，`bindtap="onRefreshTap"` 触发 `loadAll()` 并把 `refreshSpin` 置位，内层 `.refresh-icon` 播一次性 1080°（三圈）/1.5s 缓入缓出动画、文案不跟着转，`REFRESH_SPIN_MS`(1520) 后复位标记以便下次重播；`commit f12833c` 起由纯圆形改带文案）；右下 FAB ＋）。三处 pill 统一 `padding:0 18rpx; height:72rpx`。空态/错误态仍盖在地图上层（`.center-tip.empty{pointer-events:none}` + FAB z-index 更高，不挡拖拽与 FAB）。
- **`loadAll` 只拉 `/geo`**：列表分页请求随列表迁走，页面刷新链从「geo + 列表两路 Promise.all」缩成一路；`footprintsDirty`/`globalData` 跨页刷新标记随之作废（列表页自带下拉刷新，保存后重拉第一页）。
- **列表独立页 `pages/footprint-list/footprint-list`**（主包，`enablePullDownRefresh:true`）：卡片列表 + 触底分页，逻辑原样从 tab 页旧列表形态迁移（`toCard` 的 `peopleText/subText/cover`、`seq` 守卫、翻页失败按 `seq && !loadingList && page===next` 三条件回退页码）；差异是改用页面级 `onReachBottom`/`onPullDownRefresh`（原为 `scroll-view` + `bindscrolltolower`），点卡片开详情组件走完整 DTO 快路径，编辑保存与删除后都回第一页重拉（避免页码落在已被删空的区间）。本页无新增入口（新增仍只在地图页 FAB）。
- **列表页搜索 + 时间筛选（2026-09-21 加）**：顶部筛选卡（常驻，滚动不吸顶）——搜索行（放大镜图标 + 输入框 + 「搜索」按钮，非空显示 ✕ 清空）+ 月/年/全部三档 + `‹ 2026年9月 ›` 翻页与抽屉（样式与后端口径全部对齐统计页）。**关键词与生效态分离**：`keywordInput`（输入框内容）不请求，点「搜索」/键盘回车才 trim 后落成 `keyword` 并重拉第一页；✕ 清空立即重搜；时间变化即时生效。请求带 `keyword` + 按当前档算的 `from/to`（「全部」不带区间，默认档即「全部」，与加筛选前行为一致）；分页/下拉刷新沿用同一套筛选。空态分档：无筛选沿用原文案，有筛选显示「没有匹配的足迹」+「清空筛选」（清搜索并回「全部」）。周期换算抽到 `utils/footprint-period.js`（`RANGES / PICKER_COUNT / periodRange / periodLabelOf`），与统计页共用同一份。
- **列表页两态改版（2026-09-22，对标竞品三张截图）**：结构取舍是「**地图页仍全屏打底不动，列表页内做 列表/日历 两态**」（竞品是「地图」「足迹」两个 tab，我们只有 4 个 tab，不再加）。
  - **顶部栏 `.head-row`**：左侧总览文案（`70 条记录 · 68 个地方 · 3 张照片`，全局口径不随筛选变），右侧四个入口 `≡ 列表 / ▦ 日历 / 📊 统计 / ＋`（图标态，激活项 `#eef4ff` 底 + `-blue` 变体图标，每格 64rpx 见方）。统计仍是 `navigateTo` 分包统计页；**＋ 是本页新增入口**（`formRecord: null` 即 `footprint-form` 的新增态，此前新增只在地图页 FAB）。三处图标加稳定类名 `head-act-list/calendar/stats/add` 供自动化按 selector 点击。
  - **卡片整体重做**（`toCard` 字段随之换：`subText/cover` → `dayNum/monthNum/metaText/descText/photos`）：左侧日期徽章（`20 / 9月`，日去前导零）+ 标题行带 `›` + `地址 · 和同行的人`（地址优先 `address → city → name`）+ 描述两行截断 + 1–3 张照片行。列表态按 `visitDate` **分组**渲染（`2026年9月` 这类组标题；分组每次从完整 `items` 重算，故跨页同月不会重复开标题）。**竞片的「21:15」时刻与「+2」照片叠层不做**：`visitDate` 只有日期没有时刻字段，单条照片上限 3 张永远触发不了溢出。
  - **日历态**：新组件 `components/footprint-calendar`（月份标题 + `今天` + `‹ ›` + 周表头 + 网格 + `● 有足迹的日子` 图例），矩阵/平移/区间/文案/分组全部落在纯函数 `utils/footprint-calendar.js`（`buildMonthGrid`（周一起始、开头补白、末尾按实收不留整周空行）、`shiftMonth`、`monthRange`/`dayRange`、`toCountMap`、`summaryText`、`groupByMonth`），组件只 `triggerEvent('monthchange' | 'daytap')`。交互：**点某天 → 下方只列那天，再点同一天取消回整月**；`‹ ›` 换月并清掉上一月点选；未来月份禁用 `›`。日历态**隐藏**月/年/全部那一行（日历本身即时间筛选），搜索保留。
  - **切态不闪屏**：沿用本页既有约定——不清 `items`、只有首屏才进 `loading`，新数据到达后整体替换；`applyPeriod()` 按 `view` 分支决定 `from/to`，`loadCalendar()` 与列表请求分两路（`_calSeq` 独立守卫），拉不到时 toast 具体原因而不是让总览行凭空消失。
- **后端列表接口**（`GET /footprint-records`）：新增 `from`/`to`（`YYYY-MM-DD`，含 from 不含 to，走 `visitDate` 字符串 $gte/$lt，命中 `{userId, visitDate:-1}` 索引）；`keyword` 的 `$or` 在标题/描述/地点名/地址外**新增 `people` 与 `visitDate`**（搜同行的人、搜日期串）。**加权排序**：带关键词时取全量匹配后在服务内算分排序再切片分页（个人级数据量可控），权重 **标题 3 / 同行的人 2 / 描述、地点名、地址、日期各 1**，同分按 `visitDate` 降序、`createdAt` 降序；不带关键词仍走原 DB 分页（列表默认行为不变）。
- **统计页 `packageFootprint/pages/footprint-stats/footprint-stats`**（分包，复用 `ec-canvas`）：周期筛选（月/年/全部 tabs + `‹ 2026年9月 ›` 翻页 + 点标签弹抽屉选最近 12 个月/5 年，样式与交互对齐 `pages/report`）→ 中国点亮地图（echarts，同款 `getMapOption`，`visualMap.max` 按当前数据最大值动态取，**去掉**省份下钻/城市列表；分享与全屏/缩放均在 2026-09-21 补回，见下方两条）→ 数据概况（总足迹/省份/城市，首次响应落地前显示「—」防闪 0）。概况卡初期在页首，2026-09-21 按用户要求移到地图下方（状态行仍贴着地图：canvas 是独立合成层，浮层会被盖住）。`visitDate` 是 `YYYY-MM-DD` 字符串，故区间由客户端按自然月/自然年算出日期串（`periodRange`，左闭右开，复用 `utils/footprint-period.js`），不使用报告页那套 epoch ms。周期切换保留旧图、状态行显示「统计中…」，`seq` 守卫丢弃过期响应；`/geo/china-map` 按需拉取后缓存在页面实例上。
- **统计页分享导出图片（2026-09-21 加）**：底部固定操作栏（白底 + 上边框 + 安全区内边距，`z-index:50` 低于周期抽屉遮罩，抽屉打开时点不到；`.container` 留出等高底部内边距）内一个主色整宽按钮「分享图片」→ 导出**地图 + 顶部统计行**图片（如 `2026年9月 · 足迹 12 · 省份 8 · 城市 10`；「全部」档前缀换「全部时间」）→ 分享预览弹窗（`保存到相册` + `分享给朋友` open-type=share）；`onShareAppMessage`/`onShareTimeline` 标题带统计（`我的足迹统计 · 8 省 10 城`）、`imageUrl` 用刚导出的临时图（同 track-detail 的 `shareImagePath` 用法）。数据未落地点按钮 → toast「还没有数据可分享」。**导出逻辑抽到 `packageFootprint/utils/map-image.js`**（`exportChartImage(comp, opts)`，Promise 返回 tempFilePath；足迹页「分享」与省份图保存改为调它，页面内只剩 loading/toast/预览态）。
- **统计页缩放与全屏（2026-09-21 加）**：地图右下角竖排 `+` / `−` 方形按钮 + 其下「⤢ 全屏」圆形图标按钮（`.zoom-controls` / `.fullscreen-btn`，白底描边阴影、`z-index:10`，度量与足迹页同一套）。**缩放**=读 chart 当前 `series[0].zoom` 乘/除 1.3 后一次 `setOption({series:[{zoom}]})` 落地，夹在 0.5–8；**全屏**=与足迹页同款「另起一个 `ec-canvas`」（`#fsMap` 覆盖层，`z-index:10000`，左上 ✕ 关闭、右下留同款缩放按钮），进入时用当前周期的点亮数据出图、关闭即销毁 chart 引用（`wx:if` 组件销毁重建）；全屏期间卡片画布一并 `display:none`（否则穿透遮罩，见下方实测条）；全屏期间若有在途请求落地，`fetch` 会把两张图一起刷新。缩放口径抽到 **`packageFootprint/utils/map-zoom.js`**（`zoomChart(chart, factor)` + `ZOOM_FACTOR/MIN_ZOOM/MAX_ZOOM`），足迹页「点亮地图」的一对缩放按钮改为共用它（行为不变）。
- **统计页省市明细列表（2026-09-22 加）**：概况卡下方一张 `省市明细` 卡，标题右侧 `共 N 省 M 城` 汇总；一行一个省份（`圆点 + 省名 + 足迹数 + 展开箭头`），按足迹数倒序（**直接沿用接口顺序，前端不重排**，否则与地图着色口径对不上）；城市默认收起，点省份行展开其城市（缩进一行、灰色小字），**允许多省同时展开**便于跨省对比，再点收起。没有城市的省不给箭头也不进展开集合（早期直连库数据有省无市，展开了是空列表，所以 `buildRegionRows` 里 `expanded` 必须同时满足 `cities.length > 0`）。行视图换算抽到 **`packageFootprint/utils/region-list.js`**（`buildRegionRows / toggleRegion / regionSummaryText`）——WXML 算不出展开态也没法给缺失字段兜底，而展开态是 data 里的一份省名集合、每次切换整表重算。周期切换按新结果整表重建（`this._provinces` 缓存本期 provinces，展开/收起不重新请求）；某省不在本期结果里时它的展开名留在集合中也无副作用（重建后自然没有这一行）。`regionRows` 为空时整卡不渲染（空档由地图下方状态行说明）。
- **口径区分**：本页统计足迹记录（`/footprint-records/stats`，按 `location.province/city` 聚合），与既有「点亮地图」页的运动轨迹口径（`/stats/footprint`，按轨迹经过省份）互不影响、互不合并。
- **测试**：`test/footprints-page.test.js` P4/P9 改测详情半屏组件接线与浮层入口跳转、P10 测刷新按钮（置位旋转标记 + 触发重拉，动画时长过后复位）；`test/footprint-list-page.test.js`（L1~L6：字段映射、翻页追加与闸门、失败回退页码、首屏/刷新失败分档、下拉刷新、详情/表单接线；**L7~L10（2026-09-21 加）**：搜索只在确认后生效且 trim、清空 ✕ 重搜、时间档/翻页/抽屉带对 from/to 与切档归零、空态分档与「清空筛选」、带筛选翻页仍携带 keyword+from/to）与 `test/footprint-stats-page.test.js`（S1~S6 同前；**S7~S9（2026-09-21 加）**：未加载拒绝导出、导出成功落预览态并带统计行与布局参数、「全部」档前缀、导出失败兜底、转发标题与封面、保存相册成功/权限被拒去设置/其他失败分档；**S10~S11（2026-09-21 加）**：缩放按当前 zoom 乘 1.3 且夹在 0.5–8（含 chart 未就绪点按钮不炸）、全屏另起一张图并按当前周期数据出图、关闭销毁引用、全屏内缩放只作用全屏图、在途请求落地时两张图一起刷新——桩内 `echarts.init` 每次落独立实例并在 `setOption` 回写 `zoom`，以复现 `getOption → setOption` 往返；**S12~S13（2026-09-22 加）**：省市列表落行与汇总、展开/收起与多省同开、换周期重建行不留悬空展开态）；共享模块直测 **`test/region-list.test.js`**（R1~R5：保序、展开态、toggle 返回新数组、汇总文案、脏数据兜底，2026-09-22 加）、 **`test/footprint-period.test.js`**（P1~P4：自然月/自然年区间与左闭右开、文案、常量）、 **`test/map-image.test.js`**（M1~M3：未就绪 reject、白底+下移+画标题+整 buffer 导出+恢复、无统计行时纯地图导出）与 **`test/map-zoom.test.js`**（Z1~Z5：常量、按当前值缩放、上下限夹取、chart 未就绪/残缺返回 null、zoom 缺失按 1 起算）。
- **模拟器实测（2026-09-21，wechatide CLI，dev 库 70 条数据）**：地图页全屏渲染、左上「列表」「统计」pill 真实点击均能 navigateTo（坐标触控命中，说明浮层 view 在 `<map>` 之上可点）、⟳ 刷新、右下 FAB 可见；聚合气泡 13 个（34/7/7/5/4/2…）正常渲染，点簇 scale 3→5 且镜头移到簇心；详情半屏（含图片/同行/描述）与表单半屏都能盖住地图，地图侧轻量 DTO 打开详情时组件补拉 `GET /:id` 生效（描述/人物/照片从完整 DTO 渲染）；列表页卡片、下拉分页（20→40→60→70 条，`hasMore` 正确转 false）、点卡片开详情、详情「编辑」转表单并回填；统计页月/年/全部三档数字与点亮省数正确（9 月 1 条→1 省；2026 年 22 条→浙/滇/京；全部 70 条）、周期抽屉选项数与选中态、翻页箭头禁用态正确。console 无 error/warn。
- **模拟器实测暴露并已修的两处**（commit `d944894`）：① 全屏改写 wxml 时漏绑 `<map markers="{{markers}}">`，导致聚合气泡与叶 marker 全部不渲染（data 有 13 个 marker、屏上空白）——**wxml 重写后必须回归「marker 是否真的画出来」**，页级 JS 测试覆盖不到属性绑定；② 统计页 `canvas` 是独立合成层，会盖住普通 view（既有点亮地图页的省份弹窗同样被盖），故周期抽屉打开期间对画布容器 `display:none`（`visibility:hidden` 实测无效），原「统计中…」浮层遮罩改为地图下方状态行。
- **搜索/时间筛选/分享图模拟器实测（2026-09-21，wechatide CLI，dev 库 70 条）**：列表页筛选卡渲染正常（放大镜 + 输入框 + 「搜索」+ 月/年/全部 chips，默认「全部」）；点「月」→ 周期行出现「‹ 2026年9月 ›」、▶ 禁用态、列表只剩 9 月 1 条；输入「灵隐寺」点「搜索」→ 与 9 月区间叠加后无结果 → 「没有匹配的足迹」+「清空筛选」；点「清空筛选」→ 回「全部」；搜索「西湖」→ 标题命中的 4 条排在前、地址（西湖区）命中的 1 分条目沉底，**排序与权重口径一致**。统计页底部「分享图片」→ 预览弹窗内图片为白底 + 顶部统计行 + 地图下移，保存/分享按钮就位；关掉预览后地图与概况卡正常复原。点亮地图页「分享」（改走共享 util 后回归）预览正常。**API 侧另行用 `scripts/verify-list-search.ts`（不入库）直连 dev 库核对**：`西湖` 命中 24 条按 6/6/5/5/4 → 1 分排序、同分按 visitDate 降序；`张三`（同行）2 条；`2026-08`（日期串）2 条；`from=2026-08-01&to=2026-09-01` 含 from 不含 to；`keyword=打卡` + 7 月区间组合生效。
- **分享预览重影：一个模拟器合成层坑 + 一个假线索**（2026-09-21）：预览弹窗里地图出现「108% 与 96% 两套地图叠影」，起初误判为 echarts 布局过渡帧（加了 `animation:false`，叠影照旧）。真因是**画布的独立合成层盖在弹窗上**（与既有「统计页 canvas 盖普通 view」同源）——弹窗下露出来的是页面自己那张 108% 的画布。修法照搬既有约定：抽屉/预览打开期间把**内层 `ec-canvas` 置 `display:none`**（这次藏内层而不是容器，容器高度保留 → 弹层打开时下方内容不跳动）；统计页与点亮地图页都修了，实测预览干净、关闭后地图正常复原。**同时证明「看到的重影 ≠ 导出文件脏」**：直接从 `~/Library/Application Support/微信开发者工具/…/WeappSimulator/WeappFileSystem/<openid>/…/tmp/*.png` 取出真实导出文件核验，单张、无叠影，标题行与统计数字正确（`2026年9月 · 足迹 1 · 省份 1 · 城市 1`、`全部时间 · 足迹 70 · 省份 18 · 城市 30`、`点亮省份 3 个 · 轨迹 32 条`）——**后续排查导出图问题应优先核验这个文件，而不是看弹窗截图**。
- **缩放/全屏模拟器实测（2026-09-21，wechatide CLI，dev 库 70 条）**：统计页卡片图右下角 `+` / `−` / 「⤢ 全屏」渲染到位；真实点击 `+` → 中国地图明显放大、再点 `−` → 复原（缩放为 `setOption({series:[{zoom}]})` 一次落地）；点「全屏」→ 满屏地图 + 左上 ✕ + 右下同款缩放，全屏内点 `+` 同样放大，点 ✕ 回到页面且卡片图正常复原。**同时发现并修掉一个既有缺陷**：全屏白底遮罩下会露出「第二套地图」（两个南海诸岛框、卡片位置的省块重影）——真因是**卡片画布作为独立合成层穿透了全屏遮罩**（与分享预览重影同源，且**已上线的点亮地图页同样中招**，不是我这次改动引入的），修法照既有约定：全屏期间把卡片的内层 `ec-canvas` 也置 `display:none`（两页都改），复测两页全屏只剩一套地图。console 无 error/warn。
- **分享/全屏按钮的样式定稿（2026-09-21，用户连续三条指令：去文案 → 和 +− 类似 → 加透明度）**：足迹页「分享」「全屏」先由白底胶囊改成纯图标方形，再因**点击区域太小**把文案加回来，最终形态＝**图标 + 文案、横向成组的两段按钮**：`.map-actions` 容器（`right:48rpx; bottom:48rpx`，与上方纵向缩放组留 24rpx 间距）内两格，各格 `padding:8rpx 16rpx`（点击区约 116×50rpx，比纯图标方形大）、`font-size:24rpx`、图标 28rpx + 8rpx 间距，共用一层阴影/圆角与 1rpx 中缝；**统计页同样是「分享｜全屏」两格**（初版只放「全屏」，2026-09-21 用户反馈「分享按钮消失」后补上，点击调 `openSharePreview` 走同一套导出/预览；底部整宽「分享图片」按钮保留，两个入口等价）。地图控件（`.zoom-btn` 与 `.fullscreen-btn`）底色统一改 **`rgba(255,255,255,0.82)`**，让底下的地图透一点出来。实测两页点击均命中（分享开预览、全屏进全屏），透明度在浅色地图底上变化细微、需要更明显可再降不透明度。
- **修掉「数据未落地时分享出空白图」**（2026-09-21，与上面同批）：点亮地图页原先在首次响应落地前点「分享」会导出「点亮省份 0 个 · 轨迹 0 条」的空白图（模拟器实测复现：页面已显示 3 省/3 城、导出图却是 0/0）。现在 `openSharePreview` 先看 `this._mapData`（首次成功响应后才赋值），未就绪直接 toast「还没有数据可分享」并返回，口径与统计页的 `loaded` 守卫一致。冷启动点分享实测出该 toast、不再出空白图；数据落地后导出正常（「点亮省份 3 个 · 轨迹 32 条」+ 地图完整）。
- **数据观察（非缺陷，已回填）**：dev 库 48 条「全国景点」是早期脚本直连 Mongo 灌的，只有 address 没有 province/city，故「全部」曾显示 70 总足迹但只点亮 3 省（22 条经接口创建、`locateRegion` 已补省市）。真实用户经接口创建不受影响；`total` 计全部、省市只计已知，语义与点亮地图一致。**2026-09-21 已回填**：`scripts/backfill-footprint-regions.ts`（复用服务端口径 `locateRegion`，只填空值不覆盖、打印库名防误连）→ 48 条全部补上，统计页变为 70 条 / 18 省 / 30 城。该目录被 `.gitignore` 刻意排除（临时/造数脚本不入库），故只在本地留档。
- **待真机验证**：全屏地图下 marker 聚合与点簇展开的真实手指手感（模拟器为合成事件）、划动地图与浮层 pill/FAB 的误触（同层渲染真实表现）、真机 canvas 是否同样盖住弹层（若真机支持同层渲染，`display:none` 也仍正确）。

#### 浮层统一 + 筛选/搜索 + 分类（2026-09-22）

> 上面 2026-09-21 那条「左上 `.map-entries` 白底蓝字胶囊 + 右下蓝底 FAB」**已被本节取代**（留档不撤销）。动机：用户给了一张竞品参考图（地图四角全是白底黑图标圆钮），要求本页按钮风格统一，并在地图页补筛选与搜索。

- **按钮统一规格** `.tool-btn`：88rpx 白圆底（`rgba(255,255,255,.96)`）+ 40rpx 黑色 lucide 字形 + `0 2rpx 12rpx rgba(0,0,0,.14)` 投影；`.tool-btn-hover` 换 `#f2f3f5`。**2026-09-22 收口后的四角形态**：左上 = **筛选**（带生效角标），右上 = **搜索**，右下竖排 = **列表 / 新增**——原折进左上的「统计」「刷新」两个入口已按用户要求整体移除（见本节末「地图页浮层收口」条），新增按钮也不再大一档（原 `.tool-btn-add` 104/48rpx 覆写删掉，与列表同尺寸）。定位按钮本页一直没有（只有 `<map show-location>` 的原生蓝点）。**2026-09-23 右下变三格**：竖排 = **图层 / 列表 / 新增**（见下方「图层切换」条）。
- **图标资产同一口径**（2026-09-22）：浮层按钮的四个字形（`lucide-sliders-horizontal` / `lucide-search` / `lucide-text-align-justify` / `lucide-plus`）都是 96px 画布、**inset 0 满幅**出图（描边 8px、墨迹占宽 83%）。`scripts/gen-category-icons.mjs` 的 PLAIN 组原先用 `inset = SIZE*0.14`，实测墨迹只有 60% 宽、描边 5.8px，和既有的 `lucide-text-align-justify`（老资产是满幅）摆在一排上明显偏细偏小 → 改成 inset 0 并把 `search` 也纳入 PLAIN（老 `lucide-search.png` 是灰蓝色 #4a5568 的旧一代资产，一并重出成黑色 `#1f2329`；列表页搜索行共用这张，同步变黑）。`＋` 满幅后墨迹 67%（lucide 的 plus 本身只有 16 网格宽，再放就要溢出），描边与其余三个一致，视觉重量已统一。**2026-09-23 加 `layers` 时踩到反向问题**：它的菱形盘跨 22 网格，满幅出来墨迹 **88px（92%）**、比同排大一号 → PLAIN 组新增逐字形内缩表 `PLAIN_INSET = { layers: 4.5 }`，拉回 80×80 与其余三个同宽（量法同 2026-09-22：一次性纯 node 解 PNG 脚本 `/tmp/png-ink.cjs`，不入库）。同轮重跑脚本后**既有 21 张资产逐字节未变**（`lucide-static@latest` 这段时间没动过字形，`shasum -c` 全 OK），只有新的那张落盘。
- **筛选半屏 `components/footprint-filter-sheet`**：三段 chips（省份 / 年份 / 分类），每段以「全部」开头、项上带计数；**草稿态**——点 chip 只改 `draft`，`确定` 才 `triggerEvent('confirm')`，`重置` 只清草稿，再点已选中的 chip 等于取消该段。样式沿用 `.mask/.sheet` 与 `safeBottom` 抬升基准（tab 页传 `false`）。**省份 chip 只显示简称**（2026-09-22）：`utils/footprint-filter.js#shortProvinceName` 查 34 个省级单位的全量对照表（`黑龙江省→黑龙江`、`内蒙古自治区→内蒙古`、`新疆维吾尔自治区→新疆`…），**表外原样显示**；`key` 与回传给 `/geo` 的值仍是全名（要跟库里 `location.province` 精确匹配）。不走「剥行政后缀」或「截前两字」那类规则——实测字面截两字会出「黑龙」「内蒙」这种破词（用户当场否掉了第一版实现）。
- **详情半屏操作收进标题行（2026-09-23）**：用户「编辑和删除按钮放到底下有点突兀」。诊断＝两个 `size="mini"` 胶囊右对齐悬在照片下面（既不是底部操作栏也不是行内操作、没分隔线没贴底），尺寸还比弹层里其它控件小一档（64rpx vs 筛选/表单的 84rpx），且主操作与破坏性操作同一种浅底色并排、权重相同。用户在「贴底操作栏 / 收进标题行图标 / 行内文字链」里选了**标题行图标**：标题右侧依次 **铅笔（编辑）+ 垃圾桶（删除）**，内容末尾的 `.sheet-actions` 整块删除。**追加一轮（同日）**：图标先按「跟 ✕ 挤在行尾」实现，用户要求「贴着标题展示」→ 标题与两个图标包进 `.head-left`（`flex:1;min-width:0`，内部 `display:flex`），`.sheet-title` 从 `flex:1` 改 **`flex:0 1 auto` + `min-width:0`**（长标题仍出省略号，实测 40 字标题下三个图标都不掉出屏幕），✕ 留在行尾当整行的关闭（`/tmp/fp-detail-title-crop.jpg`、`/tmp/fp-detail-longtitle-crop.jpg`）。图标无文字，**颜色是唯一语义载体** → 编辑用主文字色 `#1f2329`、删除用危险红 `#e54d42`（`scripts/gen-category-icons.mjs` 新增 `PLAIN_STROKE` 表支持逐字形换色，出图名带 `-red` 后缀，同仓里 `*-blue`/`*-white` 变体的命名）；`pencil` 与 `layers` 一样跨 22 网格，满幅会到 88px，故 `PLAIN_INSET` 同为 4.5 拉回 80px 与同排齐。可点区域靠 `.sheet-act` 的 `padding:16rpx 0 16rpx 28rpx` 撑到 64rpx 见方（裸 32rpx 图标太难点）。删除仍走 `wx.showModal` 二次确认（文案点名是哪条足迹）。**接线是真实点击验的**：坐标 `automation_element_action` 点铅笔 → 详情关、表单开且带回填 DTO；点垃圾桶 → 弹确认框，点取消后 70 条数据一条没少。**踩坑**：模拟器截图→CSS 坐标的换算里，纵向偏移不是 147 而是 ~127.5（本窗口无原生导航栏），首次按旧值点击直接落空；改用「像素扫描定位图标墨迹中心 + 已知元素（右上搜索钮 bounds 355×34.5 CSS ↔ 图 540×180）反解偏移」一次命中。
- **筛选半屏按钮组贴底（2026-09-23）**：用户「底部按钮组底部空间增大一点」，改的过程中截图发现**「重置/确定」整组根本不在可视区内**——加了分类段之后三段 chips（省份 4 行 + 年份 2 行 + 分类 3 行）撑过 `.sheet` 的 `max-height:76vh`，按钮组排在滚动内容末尾，被挤到折叠线外（2026-09-22 那两张存档截图 `/tmp/fp-filter-shortnames.jpg` 回看同样是没按钮组的，属既有缺陷、不是本轮引入）。修法：`.sheet-foot` 改 **`position:sticky; bottom:0`** + 白底 + 发丝上边框，底部留白从 `.sheet` 的 `padding-bottom` **挪进按钮组自己身上**（24rpx → **40rpx**，安全区档同增），否则粘住时那截空白会被 scrollport 吃掉。**试过 `scroll-view` + `flex:1;min-height:0` 的表单半屏同款结构，这里不成立**——`.sheet` 只有 `max-height`（高度不确定）时 scroll-view 拿不到确定高度，内容从按钮组底下穿出来（截图 `/tmp/fp-sheet-foot-fixed.jpg` 可见重叠），故回退到 sticky。代价：内容滚到底之前，最后一行 chips 会压在按钮组下面（可滚动触达，按钮组始终可点）。
- **候选项在前端算**（`utils/footprint-filter.js#buildFilterOptions`）：数据源是**未过滤的 `/geo` 全量快照**，因此筛选态下 `loadAll` 会并行多发一次不带参数的 `/geo`（只在进页/刷新/增删改后发生，切筛选不重复拉）。不选「用过滤后的结果算候选」——那样选中的省份会从候选里消失，改不回去。年份取 `visitDate.slice(0,4)`，空省市与脏日期不计。
- **搜索**：右上按钮向左展开成整行输入框（`.search-dock` → `.search-dock-open`，`width: 88rpx → calc(100% - 48rpx)` + 0.22s 过渡），展开时左上竖排 `.tool-fade` 淡出且 `pointer-events:none`；复用列表页的 `keywordInput`/`keyword` **双态**（打字不请求、回车才 trim 生效，同词再回车不重拉）。命中面 = 后端 `keywordFilter` 的 `$or`：标题/描述/地点名/地址/**省份**/**城市**/同行人/日期。
- **收起与无命中提示（2026-09-22 改）**：① **✕ = 清空并收起**（原来只清字、框还开着，且框内为空时点它直接 `return` 什么都不会发生）——`onClearKeyword` 无条件 `searchOpen/searchFocus` 置 false，有词才顺带重拉；放大镜图标本身点击收起的既有路径保留。② **筛选/搜索无命中不再在地图上摆浮层文本**，改 `wx.showToast`，文案点名到具体条件（`noMatchToast`）：有搜索词 → `没有匹配「{词}」的足迹`（超 10 字截断加 `…`，toast 两行就顶到导航栏），只有省/年/分类 → `没有符合当前筛选的足迹`；**空白账号（无词无筛选）不弹**，仍留地图上的「还没有足迹，点右下角 ＋ …」引导。代价：**「清除筛选」一键入口随浮层文本一起没了**，取消筛选要重开筛选弹窗点 chip 取消或按「重置」（用户拍板接受）。
- **分类打点**：单点 marker 的 `iconPath` 由 `marker-dot.png` 换成 `fp-cat-<key>-chip.png`（白圆底黑字形，26 CSS px），未分类/未知 key 仍走灰色圆点（`config.footprintCategoryIcon` 返回空串即不覆盖）；聚合簇仍是蓝底数字气泡，展开到单点才见分类。有照片的高缩放层级仍优先出照片卡（`zoom >= 9`）。
- **图层切换（2026-09-23）**：右下三格最上一格 = **标准 ⇄ 卫星**，走原生 `<map enable-satellite>`（2.7.0+，本页基础库 3.7.0，属性可动态改），与 `track-detail` / `overview` / `record` 三页同一档位口径。**只翻底图**：`toggleLayer()` 一个 `setData({ mapType })`，不重拉 `/geo`、不重建 markers、不动相机（切数据才会闪，切底图不该）。
  - **选择记本地**：`MAP_LAYER_KEY = 'footprint_map_layer'`，`onLoad` 里 `readLayer()` 赶在首帧前回填（写在 `data` 字面量里只能给默认值），`toggleLayer` 覆盖存值；读写都 try/catch，**读失败回落标准且不拖垮进页**、写失败只是下次回默认档，不打断本次切换也不弹错。tab 页 `onLoad` 一次生命周期只跑一次，所以「记住」在冷启动/重进小程序时生效（切 tab 回来看的是同一实例，保持当前档）。
  - **形态**：本页按钮是纯图标，一个 `layers` 字形表达不了「当前是哪张底图」，故卫星档复用筛选那圈 `.tool-btn-on` 蓝环（不新造样式）；其它三页仍是「图标+文字（卫星/标准）」胶囊，两种形态各归各页语言，未强行统一。
  - **已知退化**：卫星图不显示 POI 标注 → `bindpoitap` 那条「点底图地名开详情」的兜底路径失效（点 marker / 标题胶囊 / 簇气泡均不受影响）。自绘的簇气泡、分类白圆 chip、照片卡都是白/蓝底，卫星底图上对比度实测够用。
- **图标资产**：`scripts/gen-category-icons.mjs` 拉 lucide SVG（v1.47.0，ISC）→ 包白圆底 → **本机 Chrome 无头截图**出 96px 透明底 PNG，产出 9×2 张 `fp-cat-*` 加 `lucide-sliders-horizontal.png` / `lucide-plus.png`。**踩坑两条**：① `qlmanage` 虽能渲 SVG，但会把内容缩到画布左上角，出图不可控，故换 Chrome `--headless --screenshot --window-size=96,96 --default-background-color=00000000`；② lucide 文件头那行 `<!-- @license -->` 注释里就带 `>`，用 `indexOf('>')` 找开标签结尾会截出一个没闭合的 `<svg>`，WebKit 渲染时顶部会多出错误提示条——锚点必须从 `indexOf('<svg')` 之后再找。
- **表单/详情/列表接分类**：表单在描述下面加一行分类 chips（9 类，未选中即「未分类」，再点取消；选中态换成白圆底那张图）；详情半屏与列表卡片在「日期·地点」同一行右侧挂分类小标签（`config.footprintCategoryLabel/Icon`，未分类不渲染、行高不变）。
- **兼容性**：`/geo` 不带参数时行为与历史完全一致，老版本小程序不受影响；`category` 默认 `''`，历史数据不需要回填。

### 4.3 新增/编辑表单 `components/footprint-form`（半屏弹层）

> 2026-09-21 变更（commit `d723c69`）：原独立页面 `packageFootRecords/pages/record-edit` 改为足迹页内的半屏弹层组件，两个入口就地打开——右下 ＋ 开新增态；详情弹窗「编辑」先关详情、带完整 DTO 开编辑态（详情弹窗的 record 已含描述/人物，无需二次 GET）。保存成功后组件只发 `saved` 事件，由页面 `loadAll()` 同页刷新地图+列表；随之废弃 `globalData.footprintsDirty` 跨页标记与 app 的 onShow 检查。弹层：默认高 78vh、内部 `scroll-view`；输入框 `adjust-position=false`，靠 `bindkeyboardheightchange` 抬升并缩高弹层（顶部留白 48px）+ `scroll-into-view` 把焦点字段滚进可视区（**键盘形态需真机回归**）。小程序 tab 页拦不了系统返回手势，弹层只能靠遮罩/✕ 关闭。回归：`test/footprint-form.test.js`（8 用例）+ `test/footprints-page.test.js` P9（接线）。

表单字段（顺序即视觉顺序）：地点、日期（`<picker mode="date">`）、标题、人物（输入+回车生成标签，≤10）、描述（textarea ≤500）、图片（≤3）。

- 地点：默认按钮拉起 `wx.chooseLocation`，回填 `name/address/latitude/longitude`；旁边「地图选点」入口 → `map-picker`。`requiredPrivateInfos` 增加 `"chooseLocation"`。
- 自建选点页 `map-picker`：全屏 `<map>`（参考 `components/track-map` 用法但独立实现，不动公共组件）+ 顶部搜索框（输入防抖 500ms 调 `/geo/search`，展示结果列表，点选回填）+ 直接点地图：`bindtap` 取坐标 → `/geo/reverse` 回填地址浮层 → 「确认选点」返回上一页。搜索失败/限流仅提示，不阻塞点选主路径。
- **2026-09-21 变更（commit `9777849`，用户反馈）**：自建选点页 `map-picker` 与「地图选点」入口整体移除，地点只保留「快速选点」= `wx.chooseLocation`（微信原生选点面板，免腾讯额度）；表单其余字段/提交逻辑不变。后端 `/geo/search` 路由与用例留仓，暂无前端消费方（若后续做半屏自建选点可直接复用）。上一段为留档、描述的是移除前的形态。
- 图片链路：`wx.chooseMedia({ count: 剩余名额, sizeType: ['compressed'], mediaType: ['image'] })` → 逐张 `wx.compressImage({ quality: 80 })`（失败降级用原文件）→ `api.checkImage` 合规（blocked 则移除并提示）→ 复用 `services/oss-upload.js#uploadPhoto(tempFilePath, { dir: 'footprints', prefix: 'fp_' })`。编辑已有记录时：新图走上述链路，移除的图在提交后由服务端差集清理。
- **2026-09-21 变更（commit `f657ade`，用户定稿）**：图片链路改为「编辑 → 检测 → 存档」三段，落点在 `services/oss-upload.js`：① 编辑：足迹表单点缩略图拉起 `wx.editImage`（微信原生编辑器：裁剪/涂鸦/文字/马赛克），头像在选图后自动拉起同款编辑器；已存 OSS 的旧图不可编辑（仅本地文件可传），取消/不可用一律静默回退原图。② 检测：`uploadPhoto` 先压**检测副本**——从 **q50** 起逐档下探（35/25），压进 1MB 才送 `/users/check-image`（imgSecCheck 硬限 1MB，后端 `user.routes.ts` 同值校验）；原图已 ≤1MB 或压缩不可用时直接送原图、不空转。③ 存档：检测通过后从本地原图重压 **q80 存档副本**直传 OSS（画质与检测副本解耦，OSS policy 10MB 上限此时已无实际约束）。压到底仍超 1MB 返回 `{ tooLarge, sizeBytes }`，足迹/头像/打点/轨迹详情四处调用方均按「压缩后仍有 X.XMB，超过 1MB 上限」给具体提示——此前是笼统的「照片上传失败，请重试」，且头像超限时**静默不更新**。回归：`test/oss-upload.test.js`（9 用例，stub wx + require.cache 注入 api，锁「送检用检测副本 / 直传用存档副本」与逐档下探、短路、回退各分支）。
- 提交：create 或 update；标题/日期/地点坐标缺失时禁用提交并标红。

### 4.4 详情数据流

```
tab主页 onLoad ──► GET /footprint-records/geo（地图，全屏改版后仅此一路）
表单保存成功（组件 saved 事件）──► tab主页 loadAll 重拉 geo

列表页 onLoad/下拉刷新 ──► GET /footprint-records?page=1（首屏/刷新替换整页）
                      └► GET /footprint-records?page=N（触底追加）
详情里的编辑改由详情组件带出完整 DTO，页面不再补拉；删除成功 ──► 触发 deleted 事件，页面重拉自身数据

统计页 onLoad/周期切换 ──► GET /footprint-records/stats?from&to（全部则省略区间）
                     └► GET /geo/china-map（省界，页面实例内缓存一次）
```

## 5. 安全与配额

- 所有 `footprint-records` 接口 `fastify.authenticate`；查询/变更一律带 `userId` 条件，越权返回 404。
- 腾讯 WebService 额度：本功能唯一新增消耗是 `/geo/search`（place API）。缓存在服务端进程内 + 每用户限流；`chooseLocation`、`/geo/reverse`（已有缓存）、省市补全（离线 region.ts）均不新增额度压力。
- 图片上传前后端双重校验（Zod max 3 + URL 前缀必须为本用户 OSS dir）；imgSecCheck 在客户端上传前执行，与服务端 OSS 10MB content-length 上限构成两道闸。

## 6. 测试与验证

- **后端**（`test/` 仿现有 activity 用例）：CRUD 正常流、越权 404、photos>3 拒绝、visitDate 格式、people 数量上限、防刷限流、`/geo/search` 缓存命中与限流、删除后 OSS 清理（注入 mock）。
- **小程序**：wechatide 模拟器实测全链路——tab 进入→chooseLocation 选点→压缩上传 3 图→列表卡片→地图聚合簇展开→marker 点弹窗→编辑→删除；截图验证 UI。**真机验证义务（不可删除）：聚合相关行为在开发者工具模拟器渲染能力有限，须在真机预览回归一次**；同层渲染下弹窗盖地图、真实手指点 marker/簇的热区表现同样以真机为准。
  - Task 5 修复轮实测结论（模拟器，基础库 3.7.0）：**原生 `initMarkerCluster` 已实测可用（模拟器）** —— 5 个同城点渲染为地图自带「5」字蓝底气泡、2 个异地点为普通叶 marker（`joinCluster:false`），fitBounds 视野正常，点叶 marker 弹详情弹窗（`GET /:id` 补拉字段正常渲染）。原生路线取代初版组件内网格聚合（后者已删）。**真机预览仍列为 Task 9 回归项**，重点：① `markerClusterCreate`/`markerClusterClick` 事件模拟器从未派发，最大缩放同坐标点的成员列表半屏兜底链路需真机验证；② `enableDefaultStyle:false` 的社区坑（簇点击不派发）已通过保持 `true` 规避，真机确认默认气泡观感；③ 真实手指点击（本轮为合成事件驱动）。
- 分支：两仓库 `git checkout -b feat/footprint-records`；提交/推送遵循既有流程（用户测试通过后再 push）。

#### 实现验证记录（2026-09-20，Task 9 收尾）

- **后端全量**：`npm test` → **156 pass / 0 fail / 0 skipped**。`npm run typecheck` → 11 处报错，**全部落在 `test/topic.test.ts`**（`adminReq(method: string)` 与 fastify `inject` 的 `HTTPMethods`/`InjectPayload` 推断不匹配）；该文件自 M1 骨架提交后未改动、足迹/geo 的 8 个 src/test 文件零报错，属仓库既存问题（ledger Task 1 已 triage 为「与本功能无关」），本轮不越界修改。
- **小程序全量**：`npm test`（`node --test`）→ **24 pass / 0 fail**（含 `test/footprint-geo.test.js` 的 fitBounds / gridCluster 纯函数用例）。
- **`/geo/search` 真实冒烟**：一次 dev token curl → HTTP 200 `data:[]`，服务端 `[geo] place 搜索 status= 121`（额度仍未恢复）；细节与降级语义见 §4.2 同日期记录。
- **收尾三处小修**（commit `13f7fce`，各过 `node --check`）：① `map-picker confirm()` 占位归一为 `name:'地图选点' / address:''`（原会把 `name:''`+`address:'地图选点'` 写进库，并在编辑页主行/次行双份渲染）；② `record-edit` 详情 GET 失败分支补 `refreshCanSubmit()`（慢 GET 竞态下地点已被清空却仍可点提交）；③ `footprints.syncNativeMarkers()` 在每次 `addMarkers` 前清 `_clusterMembers`（刷新后陈旧的 `clusterId → markerIds` 在真机上会开错记录；该机制已随 2026-09-21 的自绘小圆改造整体移除，此条只作当时改动的记录）。
- **模拟器密集用例（≥30 条）本轮未能执行**：微信开发者工具 MCP 通道自 22:31 起对**所有**工具（含只读 `project_list` / `check_wechatide_status`）返回「MCP tool invocation did not complete」，IDE 侧通道未恢复；同时一次性造数脚本（直连 collection、以及走 `createFootprint` 服务层两种写法）均被本机权限系统拒绝，故 28 条 `足迹T9-*` 密集点**未落库**，dev 账号仍是 22 条（7 条 `足迹T5-*` + 15 条 `足迹T6-*`，均为前序任务遗留、无一处删除）。因此本轮这六项没有新截图：簇气泡计数、点簇原生展开、叶点弹窗、下拉刷新后 marker 重投、地图⇄列表往返、编辑改名后原图保留；其判定依据回落到 Task 5（聚合渲染＋弹窗＋fitBounds 视野）、Task 6（列表往返＋触底）、Task 7（上传链；其中「编辑改名不重传原图」一项**仅有 DB 读回证明 photos 数组原样** —— `task-7-report.md` §3.2 改名后 `PUT` → GET 回读 `photos.length 1`，**network 面板证据缺失**（同报告 §3.6：`get_simulator_network` 两次返回空，IDE Network 面板不记录 `automation_evaluate` 发起的请求），故「无新增 credential 请求」未被断言过，真机回归补）的既有留证，以及本轮对三处小修的源码复核。**待 IDE 通道恢复后的动作**：先补 28 条 `足迹T9-*`（杭州西湖 0.02° 见方），再按 §4.2 真机清单回归，截图存 `.superpowers/sdd/12-足迹功能实现计划/shots/`。

#### 实现验证记录（2026-09-21，地图全屏 / 列表独立页 / 统计页）

- **后端**：`test/footprint-record.test.ts` 13 pass / 0 fail（含本轮新增 stats 用例：省/市口径与排序、区间过滤（to 不含）、空窗口零值、`from >= to` 与非法格式 400、未登录 401）；`npm run typecheck` EXIT=0。dev 库有 70 条 `足迹T6-*` 测试数据（前序任务遗留）。
- **小程序**：`npm test`（`node --test`）→ **64 pass / 0 fail**。构成：`footprint-geo` / `oss-upload`(9) / `footprint-form`(8) / `tracker` / `footprints-page`(P0~P10，P10 为刷新按钮动画标记) + 本轮新增 `footprint-list-page`(L1~L6) 与 `footprint-stats-page`(S1~S6)。
- **浮层图标与刷新动画（2026-09-21 补测，wechatide CLI）**：三个 pill 全部换 lucide 风格图标（列表 `text-align-justify` / 统计 `chart` / 刷新 `refresh-cw`）与统一 18rpx 内边距，模拟器截图确认版式；刷新点击动画因瞬时 1.5s 抓不到中间帧，先把时长临时调到 6s 抓两帧（箭头分别指向左上 / 右下，确认为真实旋转而非静态图），随后改回 1.5s/1080°。**踩坑：`automation_element_action` 的 `--x/--y` 是「视口 CSS 像素」而非截图像素**（截图含状态栏与导航栏、且被缩放 ~1.52 倍），首次按截图坐标点击落空；稳妥做法是 `--action tap --selector ".map-refresh"` 按选择器点击。
- **未在本轮验证**（本机 wechatide MCP 通道当前不在可用工具列表，模拟器整块矩阵无法执行）：全屏地图下的 marker 聚合手感与左上浮层 pill / FAB 的同层渲染层级、统计页 echarts 在分包内的首屏渲染、列表页下拉刷新与触底翻页的真实手势 → 交用户在开发者工具/真机确认。页级/接口级逻辑已由上述回归覆盖。

#### 实现验证记录（2026-09-22，列表页两态改版 / 日历态 / `/calendar` 接口）

- **后端**：`test/footprint-record.test.ts` 19 pass / 0 fail（本轮 +3：days 计数与 `placeCount` 去重/空名不计、归属隔离（同一天同地点名各塞一条他人数据断言不串）、空态全零 + 未登录 401）；`npm test` 180 pass / 0 fail（原 177），`npm run typecheck` EXIT=0。**先跑红确认过**：新路由注册前 `GET /calendar` 会落进 `/:id` 并抛 `Cast to ObjectId failed for value "calendar"` 返回 500。
- **口径用 dev 库真实数据核过**：某用户 70 条 → `total 70 / placeCount 68 / photoCount 3 / days 67`（两处重名地点被正确去重），读作「70 条记录 · 68 个地方 · 3 张照片」。
- **小程序**：`npm test` → **109 pass / 0 fail**（原 94）。新增 `test/footprint-calendar.test.js`（C1~C8：月历矩阵补白与行数、三态标记互不覆盖、跨年平移、左闭右开区间与文案、总览缺项不硬凑、分组保序、「其他」组兜脏数据、`toCountMap` 累加）与 `footprint-list-page` 扩到 L1~L17（新增 L11 总览与失败 toast、L12 切态带整月区间且不清 items、L13 点某天与再点取消、L14 换月清点选、L15 统计跳转与 ＋ 新增态、L16 保存/删除两处一起刷、L17 三档空态文案）。
- **顺带修掉一条存量红**：`footprint-form.js` 的 iOS 键盘修复会调 `wx.pageScrollTo(...).catch()`，而 `test/footprint-form.test.js` 的 wx 桩没有这个方法 → 该用例在改动前就是失败的（组件与测试都是 HEAD 原样，与本轮无关）。补桩为返回 Promise 并加断言「键盘开合各复位一次」。
- **图标资产**：`assets/icons/lucide-calendar.png` / `lucide-calendar-blue.png` 为新增（lucide `calendar` 描边规格：圆角矩形主体 + 两个挂环 + 分隔横线，24 视口、81px 出图、4× 超采样后 LANCZOS 缩回）。**线宽要对齐既有资产**：lucide 原 2.0 描边在 81px 上实测 8px，而库内 `lucide-chart/maximize/share` 都是 5–7px，故取 1.6 视口单位（出图 6px）。
- **模拟器实测（wechatide MCP，dev 库 70 条）**：列表态顶部栏渲染出「70 条记录 · 68 个地方 · 3 张照片」（证明 dev 服务 `tsx watch` 已热重载新路由）、月份组标题 `2026年9月/8月/7月`、新卡片（徽章 `1 / 9月` + 地址·同行 + 描述 + 两张照片）；切日历态 → 时间筛选行消失、`2026年9月` + `今天` + `‹ ›`（`›` 在当前月禁用）、9/1 打点变蓝、今天描边、图例就位、「本月 1 条」+ 卡片；点 `‹` 到 8 月 → 打点变 17/20、`›` 转蓝可点、标题「本月 2 条」；点 8/20 → 实心蓝圆 + 「8月20日 · 1 条」只剩一条；再点同一天 → 回到「本月 2 条」；点 ＋ → 半屏「新增足迹」（地点/日期 2026-09-22/标题/描述/同行的人）；点 📊 → 落到「足迹统计」页（2026年9月、浙江点亮、1/1/1、`分享｜全屏` 与 `+/−` 就位）。列表态 `pageScrollTo` 连翻两页到底部（2024年12月→11月→10月→6月）无重复组标题。console `grep -iE "error|warn|fail"` 空命中。
- **踩坑（补 §4.2 同条）**：**custom component 内部元素页面级 selector 选不到**（`.cal-arrow` 返回 `no such element`），组件内交互只能按坐标点；坐标换算沿用 `css_x = 截图x/1.521`、`css_y = (截图y − 147)/1.521`（本机模拟器视口 390 CSS px，截图宽 593），本次按该式换算的 `‹` 箭头与日期格两次点击均一次命中。另：坐标点击会在页面上留下 DevTools 元素高亮的蓝色遮罩（截图左下角 `body. 390 × 764.67` 角标），是工具态不是页面样式。

## 7. 不做的事（YAGNI）

- 不做足迹公开/分享/点赞；不做多边形/行政区划选点；不做照片 EXIF 自动取时间地点；不做足迹与运动轨迹数据打通。
- ~~不做 webAdmin 足迹管理页~~ —— **2026-09-22 已立项并落地**（合规图误放行需要人工处置入口，见文末「webAdmin 足迹管理」记录）。
- **竞片的三处形态明确不抄**（2026-09-22 评估）：① 卡片上的到访**时刻**（`visitDate` 只有 `YYYY-MM-DD`，加时刻要动 schema + 表单 + 全部口径）；② 照片行的 **`+N` 溢出叠层**（单条上限 3 张，放不开也不做）；③ ~~地图**分类图标 marker**（陵/庙/山按类型着色，需新增 `category` 字段与枚举，另立项再说）~~ —— **同日已落地**（见 §4.2「浮层统一 + 筛选/搜索 + 分类」）。tab 结构也保持「地图页打底 + 浮层入口」，不改成竞品的「地图 / 足迹」双 tab。
- **分类不做成后台可配**（2026-09-22）：枚举写在代码里（后端 key+中文名 / 小程序 key+图标 / 管理端 key+中文名三份）。图标是打进小程序包的静态 PNG，做成 CRUD 就得配套图片上传、包体与缓存策略，成本不对等。
- **筛选弹窗的「分类」段不含「未分类」**：只列数据里出现过的 key，`''` 不计入（否则与「全部」语义重叠）。真要按未分类筛，得给 `/geo` 加 `category=none` 的显式口径，本轮不做。
- **地图页筛选不共享给列表页**：本轮筛选/搜索只加在地图页（用户点名的是地图），列表页保留自己那套「关键词 + 月/年/全部」；两边参数命名与双态交互一致，但省/年/分类三段筛选没往列表页搬。
- **统计页省市列表只做展示，不做跳转**（2026-09-22）：点省份/城市不跳地图页筛选、不跳列表页。要做就是跨页传筛选态（地图页已有 `province` 三段筛选，但入口在页内），且城市这一维地图页筛选还不支持——两页口径得先对齐，另立项再说。

#### 实现验证记录（2026-09-22，webAdmin 足迹管理 / 用户详情足迹 Tab / 数据概况足迹指标）

**立项理由**：`wechat-sec` 两条降级放行（微信接口异常或未配置时 `skipped` 直接放过）+ `checkText` 只护昵称（标题/描述/同行的人不送检），意味着违规内容确实可能落库，需要人工处置入口。§7 原「不做 webAdmin 足迹管理页」作废。

**命名边界（务必别混）**：`/admin/users/:id` 的 `footprint` 字段与 `FootprintMap` 组件一直是**轨迹点亮城市**口径（`services/footprint.ts`）；本轮管理端「足迹」全部指用户自录的 `footprint-records`，接口 `/admin/footprint-records*`、前端类型 `FootprintRecordItem` / `FootprintRecordDetail`、页面「足迹管理」。两套口径互不影响。

**API（3 新 + 3 扩）**：
- `GET /admin/footprint-records`：`page/pageSize(≤100)/userId/keyword/province/minPhotos/visitFrom/visitTo`，固定 `createdAt` 倒序；`keyword` 除文本命中（标题/描述/地点名/地址/同行）外，还整体纳入**昵称命中用户**的全部足迹（后台常按人找记录，与 `/admin/activities` 的昵称搜索同口径）；列表**不下发 photos 数组**（100 条 × 3 图 = 300 个签名 URL 白烧 CPU），只给 `photoCount`；`userUid` 与用户列表同为字符串口径，`openid` 不外泄。
- `GET /admin/footprint-records/:id`：正文 + 签名照片 + 归属人昵称/UID。
- `DELETE /admin/footprint-records/:id`：硬删 + OSS 清图。服务层抽出 `deleteFootprintById(id)`（不校验归属），用户端 `deleteFootprint(id, userId)` 改为「先 `findOwnedRecord` 过归属闸门 → 再委托」，OSS 清理逻辑只留一份。
- `/admin/overview` +`footprintCount/footprintUserCount/footprintPhotoCount`；`/admin/stats` 每档 +`newFootprints`；`/admin/trend` 每桶 +`newFootprints`（与 users/activities 同一个 `idExpr` 分桶）。
- 测试：新增 `test/admin-footprints.test.ts`（F1~F9：排序回填/六类筛选可叠加/分页夹取与越界/鉴权 401/非法 userId 404/详情字段/详情与删除的 404 与鉴权）与 `test/admin-footprint-metrics.test.ts`（4 条）；`npm test` → **199 pass / 0 fail**（原 186），`npm run typecheck` EXIT=0。
- **踩坑三条**：① dev 库里躺着 70 条真实足迹，任何「总数/排序」绝对断言都必须把结果集锁在种子内（昵称与标题共用唯一前缀、省市用 `测试省X` 这类库里查无的值），否则 `province=浙江省` 会捞出 29 条既有数据；② 照片数下限不能用 `photos: { $gte: n }`（那是「元素值 ≥ n」），只能 `filter.$expr = { $gte: [{ $size: { $ifNull: ['$photos', []] } }, n] }`；③ trend 的桶值由 `$dateToString`（UTC）产出、桶标签由本地时间 getters 生成，本地 00:00–08:00 创建的记录会归进前一个桶 → 逐桶断言会随时钟抖动，改用「全桶求和差值」+ 一个远离边界的固定桶（`2024-H1`）。

**webAdmin**：`src/api/index.ts`（4 方法 + 4 类型 + 三处响应类型扩字段）、`src/pages/Footprints.tsx`（抄轨迹列表骨架：关键词/省份/照片数/到访日期区间 + 查询·重置 + 分页表）、`src/components/FootprintDetailDialog.tsx`（自取数 + 三列指标网格 + 描述保换行 + 照片三列宫格 + 「查看该用户」+ 删除二次确认）、`App.tsx` 路由 `/footprints`、`BasicLayout.tsx` 菜单、`UserDetail.tsx` 第三个 Tab（复用列表接口传 `userId`，按 `[id, activeTab]` 懒加载，换用户先清行防串数据）、`Dashboard.tsx`（概况 +3 卡、周期格 +「新增足迹」、趋势第三条柱）。该仓库无测试框架，验证＝`tsc -b && vite build` 通过 + `oxlint` 0 error（仅 8 条与既有同族的 `exhaustive-deps` warning）+ 浏览器实测。
- **浏览器实测（dev API 3004 + vite 5177，`admin` 登录）**：数据概况 7 卡（足迹 70 / 记录用户 1 / 照片 3）与 15 个周期格、趋势图三柱（09-20/21 出现橙色足迹柱）；足迹管理列表 70 条、关键词 `web核验` 收敛到 1 条、详情弹窗字段/描述换行/照片三宫格（真实 3 图加载成功）；**删除链走通**：二次确认文案含「3 张照片一并从 OSS 清除，不可恢复」→ 确认 → 弹窗关、列表刷成 `足迹列表（0）` + 空态，DB 复查 0 条残留（该条为本轮临时造的核验数据，删除即清理）；用户详情「足迹记录（70）」Tab 懒加载出表；深色模式下新页面配色随 `--td-*` 变量走、无白底残留；~~console 无 error/warn~~ **该结论当时不成立，见文末「DateRangePicker 报错」修正记录**。
- **部署顺序**：先 API（新接口 + 扩字段）后 webAdmin，否则新页面 404、概况取不到字段。
- **本轮仍不做**：删除审计表（谁删的）、批量删除、管理端足迹点亮地图（统计页在小程序侧已有）。

**同日补：足迹管理页顶部「足迹数据概况」**（用户追加指令「足迹列表上方也要加上足迹数据概况」）
- 新接口 `GET /admin/footprint-stats`：照 `/admin/activity-stats` 的形态，**一次 `$facet` 返回 today/week/month/year/all 五档**，每档 `{total, userCount, provinceCount, cityCount, photoCount, withPhotoCount}`；时间一律按 **`createdAt`**（到访日期是用户手填的，讲不清「这段时间进了多少数据」），今日 0 点走 `bjToday0()` 东八区口径。城市按 **`省|市` 组合**去重（同名市跨省算两个），空省市不计入省市数但计入 total。
- 前端切档**不再发请求**（五档已在同一 payload 里），也就不会闪屏；`stats` 未落地时四格与补充行显示「—」而不是 0，人均在 `userCount=0` 时显示「—」不做除零。删除一条后同时刷列表和概况（五档都得重算）。
- 概况只吃时间范围，**不跟随列表筛选**（关键词/省份/照片数只作用于列表）——与轨迹管理页同一分法。
- 顺手把时间范围按钮组抽成 `src/components/RangeButtons.tsx`（选项由各页传入），足迹页与轨迹页共用；`UserDetail` 那组因档位键不同（`total` vs `all`）未合并。
- 测试：`test/admin-footprint-metrics.test.ts` +3 条（五档窗口边界 1/1/1/2/3、省市去重与空值、用户数去重、照片数与「有照片条数」多图不重复计、未登录 401）；`npm test` → **202 pass / 0 fail**（原 199），`typecheck` EXIT=0。浏览器实测：今日档全 0 而累计档 70/1/18 省/3 图（与 `/admin/overview` 的 70 与 3 对得上），切档零请求，轨迹管理页回归正常。

**同日再补：足迹省份分布 + 足迹趋势**（用户追加指令「足迹省份分布也加一下吧，类似轨迹关联。足迹趋势也加一下」）
- `GET /admin/footprint-geo-stats?range=` → `{range, total, provinces:[{province, count, cities:[{city,count}]}]}`，形态与 `activity-geo-stats` 一一对应：按 `location.province/city` 聚合、**空省市的记录不入图**（所以分布 `total` 可能小于概况 `total`，卡片标题写成「18 省 / 70 条」把两个数都摊开）、省份按 count 倒序、省内城市按 count 倒序、非法档位原样回显并回落成累计窗口。
- `GET /admin/footprint-trend?days=` → `{days, data:[{date, count, photos}]}`，按 `createdAt` **东八区分桶 + 补零**（`$dateToString ... timezone:'+08:00'`），`days` 夹在 7–365、非法/缺省回落 30。注意与轨迹侧的差异：`activities.startTime` 存的是 **epoch ms 数字**（所以 `activity-trend` 聚合里要 `$toDate`、`$match` 直接比数字），而 `footprintrecords.createdAt` 是真 Date，`aggregate` 的 `$match` 不做 schema 转型 → 这边必须 `new Date(start)`。
- 前端排布对齐轨迹管理页：概况卡 → 并排「足迹趋势（近 30 天）」3fr + 「足迹省份分布」2fr → 列表卡。趋势柱=新增足迹条数、折线=新增照片数（同为计数，量级差 ≤3 倍不开右轴）；地图直接复用 `FootprintMap`（`valueLabel="足迹数"`，保留省份下钻看城市）。**趋势固定近 30 天、地图跟随右上角时间档** —— 两点都与轨迹页现有行为一致。删一条会同时刷列表/概况/趋势/分布。
- 挂载只取一次分布（`[range]` effect 自带首取，mount effect 里不再重复调，避免同一请求发两遍）。
- 测试：`test/admin-footprint-metrics.test.ts` +5 条（层级与计数、空省市不入图、省/市倒序、range 窗口与非法档回落、补零与窗口边界、days 夹取四档、两接口未登录 401）；`npm test` → **207 pass / 0 fail**（原 202），`typecheck` EXIT=0。
- 浏览器实测：卡片标题「足迹趋势（近 30 天）」「足迹省份分布（18 省 / 70 条）」，两张 canvas 均有实际绘制像素（5.6% / 15.7% 非透明）；趋势非零桶 `09-20:22 条/3 图`、`09-21:48 条/0 图` 合计 70 与总数对得上；resource 计数确认「挂载各一次、切档只多发一次 geo、概况与列表不重复请求」。**截图这一步本轮被本机权限分类器拦下**（`take_screenshot` 拒绝，`select_page bringToFront` 后仍拦），页面视觉请自行在 5177 端口过一眼或放开权限后我补图。

**同日再补：用户详情页「该用户足迹概况」**（用户追加指令「个人详情页，足迹列表上面也加上足迹的数据概况」）
- 接口不新增：`GET /admin/footprint-stats` 加可选 `?userId=` 收口到单个用户（不带仍是全站），非法 id → 404 `用户不存在`。**踩坑（本轮唯一一条真 bug，由测试先跑红抓到）**：`aggregate` 的 `$match` 不按 schema 转型，直接把 24 位 hex 字符串塞进 `userId` 会**恒不命中、静默返回全 0**，必须先 `new Types.ObjectId(...)`。
- 前端把概况卡抽成 `src/components/FootprintStatsPanel.tsx`（`title` 可换文案、`range` 由父级持有），足迹管理页与用户详情页共用同一份版式，不再抄第二张卡；`Footprints.tsx` 原地改引用（内联卡 JSX、`STAT_RANGES`、`sec/num/perUser` 随之删除）。用户详情页：`fpStats/fpRange`（默认累计）随「足迹记录」Tab 懒加载取数，换用户置 `null`（否则「—」不出现会挂上一个人的数），删除一条走同一个 `loadFpStats()` 一起刷。
- 单用户口径下「记录用户数」恒为 1、「人均」等于总条数 —— 为守住跨页形态统一没有做分支裁剪，若要去掉两格说一声。
- 测试：`test/admin-footprint-metrics.test.ts` +1 条（userId 与时间档叠加、ghost id 全 0、`?userId=abc` 404），该文件 13/13；`npm test` → **208 pass / 0 fail**（原 207），`typecheck` EXIT=0。webAdmin `tsc -b && vite build` 通过、`oxlint` 0 error（warning 仍 9 条、与既有同族）。
- 浏览器实测（`/users/6a7ab349398adc2c86b4c1ba`）：「该用户足迹概况」渲染在列表卡上方并与列表卡同宽同边距（x=224/w=880），四格各 196px 不换行、补充行单行；累计档 70/1/18/3 与切到今日档 0 全对，**切档零请求**、整个 Tab 只发 1 次 `footprint-stats?userId=…` 与 1 次 `footprint-records?page=1&pageSize=10&userId=…`。注：dev 库只有这一个用户有足迹，故全站数与单用户数相同，收口逻辑正确性以 API 测试（造第二个用户）为准。截图仍被本机权限分类器拦（同上一轮）。

#### 实现验证记录（2026-09-22，管理端图片签名漏口 + 列表图片列）

**立项理由**：用户报「管理后台的图片加载好像都有问题，没有接 auth_key」。实测**不是 auth_key**（全仓无此实现），本项目图片一直是「私有桶 + `getSignedUrl` 签名链」；裸链直连 dev 桶实测 `403 AccessDenied`，所以任何把裸 URL 下发的地方都是裂图。小程序不裂是因为它走的接口都签了名。

**修了三处裸发 + 一处自我污染**（API）：
- `GET /admin/users/:id` 的 `avatarUrl` 裸发 → 走签名。用户详情页头像是这一类。
- 管理端专题列表的 `content` 内联图不签名（`signContentImages` 此前只用在小程序侧接口）→ 后台 markdown 预览全裂。现在同一口径。
- 顺带修掉签名链回写入库的死循环：列表下发的是签名链，编辑保存原样 PUT，而写入侧不净化 → **签名串（含 `Expires/Signature`）落库**，24h 后必裂。现在 `coverUrl` 与正文内联图在 `validateInput` 里统一 `cleanUrl` 剥参数（`uploadBuffer` 返回的 `url` 仍是裸链供入库，新增 `previewUrl` 供展示）。
- 足迹/轨迹详情此前「图能出」是假阳性：种子照片是 `wx.qlogo.cn` 外链，`getSignedUrl` 对外链原样放行。真 OSS 图（`marker_*.webp/png`）签名后实测 `200 image/png`，裸链 `403`。

**列表图片列（API + webAdmin）**：
- `/admin/activities` items +`photoCount`+`coverPhoto`：分页后按本页 id 回表只捞 `markers.photos/markers.photoUrl`（不放开头条的 `-markers`）；**跨 markers 按裸链去重**，`photoUrl` 与 `photos[0]` 同图不双计，老数据只有 `photoUrl` 算 1 张，外链图也计数。
- `/admin/footprint-records` items +`coverPhoto`（签名首图），仍不下发 `photos` 数组；整组照片由预览按需取详情。
- 新组件 `src/components/PhotoCell.tsx`：36px 缩略图 + 「N 张」 + 预览按钮 → tdesign `ImageViewer`（`共 N 张` + 左右切图）；缩略图取不到时换成「缺图」占位并写明原因（dev 库里就有一条 `marker_*.webp` 是 `404 NoSuchKey` 的历史脏数据）。四处表格共用：足迹管理列表、用户详情「足迹记录」、轨迹管理列表、用户详情「轨迹记录」。

**测试**：新增 `test/admin-image-urls.test.ts`（头像签名、photoCount 去重四张、coverPhoto 签名与无图空串、未登录 401）+ `test/topic.test.ts` 追加 3 条（coverUrl 写入净化、列表 content 内联图签名、正文写回净化）；先跑红确认过失败原因（头像返回裸链、`photoCount` 为 `undefined`）。`npm test` → **215 pass / 0 fail**（原 208：新文件 4 条 + 专题 3 条），`npm run typecheck` EXIT=0。webAdmin `tsc -b && vite build` 通过、`oxlint` 0 error（9 条既有同族 warning）。
- **浏览器实测（真数据）**：用户详情头像由裂图变出图（signed，998px）；轨迹管理列表扫全站 3000 条只有 1 条带打点照片，该行显示「2 张」+ 预览，点开 `共 2 张`、其中 png 正常渲染、webp 命中 `404 NoSuchKey` 显示组件自带失败态；缩略图位显示「缺图」占位。足迹管理列表筛「有照片」后两行分别是 `1 张 / 2 张` + 预览，点开也是 `共 2 张`（两条照片链都是 `wx.qlogo.cn` 外链 → 签名逻辑原样放行，两条都能出图）。console 无 error。
- **dev 环境观察（未复现，记录备查）**：页面刚加载后第一次手发 `fetch` 常返回 401 `缺少管理员凭证`（即 `authorization` 头为空），紧接着同样请求又 200；app 自身请求不受影响。与本轮改动无关，若复现再看。

#### 实现验证记录（2026-09-22，端上图片改缩略图档）

**立项理由**：管理端图片列落地后，用户追加「小程序端渲染的图片也改为缩略图，点击查看时再渲染原图」。实测样本：打点照片原图 996.6KB，`w_240/quality,q_80/format,jpg` 后 **9.4KB**（106 倍），头像 `m_fill,w_160,h_160` 5.2KB，全宽图 `w_800` 45.7KB。列表一屏 10 条 × 3 图原本要拉 ~30MB。

**签名档位收口在后端**（`src/services/oss.ts`）：`getSignedUrl(url, expires, process)` 加第三参 + `getThumbUrl/getAvatarUrl/getMediumUrl` 三个封装 + `IMAGE_PROCESS` 常量表。`x-oss-process` **进签名串**，前端自己拼必然签名失败，所以只能后端出档。统一转 `format,jpg`（不转 webp：iOS 小程序的 webp 支持不打包票）。顺带把 `getSignedUrl` 里每次调用 `new OSS(...)` 改成惰性单例 —— 一条详情要签十几条，加缩略图就是翻倍。外链（`wx.qlogo.cn` 头像等）四档都原样放行，不进处理链。

**下发字段**（原则：原图与缩略图并存、同序，端上渲染 thumb、`wx.previewImage` 用 origin）：
- 足迹 `toDto` 加 `photoThumbs`（列表与详情都给）。**列表仍保留 `photos` 原图档**：已发布的老版本小程序读的是 `photos`，砍掉等于赌用户升级 —— 新端不请求原图就已经拿到全部收益，没必要为此做接口版本。
- `/footprint-records/geo` 加 `coverPhotoThumb`（地图气泡照片卡是 108×64 的 canvas 合成）。
- 轨迹详情 `markers[].photoThumbs`（`photos` 仍是原图；老数据只有 `photoUrl` 时按一条算，与 `photoCount` 同口径）。
- 头像一律 `getAvatarUrl`：`/users/me`、登录返回、排行榜行（含管理端共用同一 service）。`my.js` 那套 6h 头像缓存按「去 query 的裸链」做 key，换档不影响命中。
- 专题：列表封面 thumb、详情封面与正文内联图 medium（全宽展示 240 会糊）；管理端列表封面 thumb、正文 medium。
- 管理端列表 `coverPhoto` 也换成 thumb（36px 格子）。

**端上改造**：`footprint-list` 卡片照片行 → `card.photoThumbs`；`footprint-detail` 半屏格子 → `detail.photoThumbs`，`previewPhoto` 改为「有原图直接预览原图 / 只有缩略图的 DTO 才补拉详情换原图并并回 detail」，取不到地址时给具体 toast 不再静默 return；`marker-form` 新增 `existingThumbs` 与 `existingPhotos` **按下标一一对应**（`removeExistingPhoto` 必须同时剔两串，否则缩略图串到别的照片上），格子渲染 thumb、点格子 `previewExisting` 走原图；`footprints` 地图照片卡三处 `coverPhoto` → `coverPhotoThumb`。

**测试**：API 新增 `test/oss-thumbnails.test.ts` 7 条（三档参数与签名、外链放行、足迹列表两档并存且原图不带 process、geo thumb、markers 同序、头像三处、专题两档），先跑红确认过失败原因；`npm test` → **222 pass / 0 fail**（原 215），`typecheck` EXIT=0。小程序新增 `test/marker-form.test.js`（5 条：两串对齐、老数据只有 photoUrl、删一张同步剔除、预览用原图、无图给具体文案）与 `test/footprint-detail-sheet.test.js`（5 条：列表卡快路径不发请求、geo 补拉一次、预览用原图串、缺原图才补拉、补拉失败 toast 具体原因），`footprint-list-page` 的 L1 改成 thumb 断言；`npm test` → **119 pass / 0 fail**（原 109）。marker-form 那条「两串对齐」做过变异验证：把 `existingThumbs.push(thumbs[i] || u)` 改回 `push(u)` 后 3 条转红，确认用例真的咬得住。
- **模拟器实测**：足迹列表（dev 70 条）照片行按 `photoThumbs` 正常出图；轨迹详情「编辑打点」的已有照片格子里，OSS png 渲染的是 9.4KB 缩略图档，点它 → 原生预览器 `2 / 2` 显示 996KB 原图（清晰、可缩放）；`/footprint-records/geo` 在端上确实带回 `coverPhotoThumb`（70 条里 2 条有图）。
- **未覆盖**：地图气泡照片卡的 canvas 合成只在 dev 外链图上验过字段（外链 thumb==原图，看不出档差），真 OSS 覆盖的放大效果需真机确认；`marker-form` 里那条 `marker_*.webp` 对象在桶里已不存在（`404 NoSuchKey`），格子显示为空 —— 与上一轮管理端「缺图」同源，是历史脏数据，端上要不要也加个「缺图」占位等你定。

**修正：管理端 DateRangePicker 报错**（用户贴出 console 报错，2026-09-22）
- 现象：`/footprints` 每次渲染都刷 `TDesign DatePicker Error: typeof value: undefined must be Array!`。
- 根因：tdesign-react 1.18.2 的 `useRangeValue` 里是 `if (!Array.isArray(value)) log.error(...)` —— `undefined` 也算不合法，而我在足迹页写的是 `value={visitRange ?? undefined}`（想表达「空」）。**用户详情「登录记录」Tab 那台 picker 是同一写法、同一报错**，属上一轮就带进来的存量问题，不是足迹页新引入。
- 修法：空态一律给 `[]`（`isValidDate([])` 走 `[].every()` 恒真，两条校验都过），两处各改一行。
- 复测：`/footprints` 载入 + 选区间（请求带上 `visitFrom=2026-09-05&visitTo=2026-09-20`）+ 点「重置」回到空态，console 全程只有 vite/React DevTools 两条 info；用户详情「登录记录」Tab 出表 20 行、picker 空态无报错。`tsc -b && vite build` 通过、`oxlint` 0 error。
- 顺带纠正上一轮的记录：那处「console 无 error/warn」是漏判（当时的抓取没有覆盖到这个组件渲染期报错，原因未深究），已在那条记录上标注作废；以后管理端验 console 一律走一遍「进页面 → 开弹窗 → 用一遍筛选器 → 重置」再下结论。
#### 实现验证记录（2026-09-22，地图页浮层统一 / 筛选 / 搜索 + 足迹分类）

- **后端**：`npm test` → **230 pass / 0 fail**（原 222），`npm run typecheck` EXIT=0。新增 `test/footprint-category-geo.test.ts` 8 条：zod 分类闸门（9 key + `''`，非法值的 400 报文断言含实际值与中文清单）、category 在 创建/详情/列表/geo 四条读路径一致下发、编辑可改可清空、geo 的 province/year/category 三过滤器各自生效且可叠加（无命中回空数组而非全量）、geo 关键词命中省份与城市、列表关键词同样命中省市（共用一套 `$or`）、`year=20xx/20241` 与非法 category 400、不带参数仍是全量且 item 带省市（老客户端兼容）。**先跑红确认过**：`FootprintGeoQuery` 未导出时整个文件 `SyntaxError: does not provide an export named`。
- **小程序**：`npm test` → **137 pass / 0 fail**（原 119）。构成：新增 `test/footprint-filter.test.js`（F1~F5：候选按计数/名称/传入顺序排、脏日期与空省市不计、query 只带非空项且 keyword trim、关键词不算「筛选中」）、`test/footprint-filter-sheet.test.js`（S1~S5：打开回填三段、chip 文案与图标与计数、草稿态与再点取消、重置不立即生效、无数据态）、`footprints-page` 扩到 **P11~P14**（筛选确定按 query 重拉 + 另拉全量算候选 + 角标计数、清除筛选归零、搜索双态与同词不重拉、单点 marker 用分类图标而未分类仍走圆点）、`footprint-form` +2（分类 chips 渲染/取消、POST/PUT payload 带 category）、`footprint-detail-sheet` +1、`footprint-list-page` +1（L18 卡片分类字段）。
- **P7 当场抓到一个真 bug**（改 `loadAll` 时）：`buildFilterOptions(snapshot)` 传的是 `/geo` 的**响应对象**而不是 `items` 数组 → `for...of` 抛 `TypeError`，被 `loadAll` 的 catch 吞成整页错误态、屏上 0 个 marker。P7 断言「loading 收起时 markers 已在」把它钉住了；修法是把快照取成 `shown.items`。**教训**：`loadAll` 的 catch 会把这类编码错误降级成「加载失败」文案，页级测试必须断言到「渲染出了东西」而不只是「没抛异常」。
- **管理后台**：`tsc -b && vite build` 通过；`oxlint` **9 warnings / 0 errors**（全是既存的 `react-hooks/exhaustive-deps`，本轮未新增）。改动是 `FootprintRecordItem/Detail` 加 `category`、足迹列表加「分类」列、详情弹窗加一行，中文名单一出处 `src/utils/footprintCategory.ts`。
- **图标资产**：`scripts/gen-category-icons.mjs` 出 20 张 PNG（9 张透明底字形 + 9 张白圆底 + `lucide-sliders-horizontal` / `lucide-plus`），单张 617B–4.5KB，均 `hasAlpha: yes`、96×96。目检 `fp-cat-heritage-chip.png`（白圆 + 黑城堡）与 `fp-cat-museum-chip.png` 字形清晰居中。
- **模拟器实测（wechatide MCP，dev 库 70 条真实数据）**：
  - 四组按钮渲染为白底黑图标圆钮：左上竖排 筛选/统计/刷新、右上 搜索、右下 列表/新增（`/tmp/fp-map-default.jpg`），全国视野下 13 个自绘蓝底数字簇正常。
  - 点筛选 → 半屏「筛选足迹」真实数据到位：**省份** 18 项带计数（`全部 / 浙江省 29 / 北京市 7 / 上海市 4 / 陕西省 3 …`）、**年份** `全部 / 2026 年 22 / 2025 年 12 / 2024 年 18 / 2023 年 12 / 2022 年 6`、**分类** 段只有「全部」（dev 库全部未分类，符合预期）；底部 `重置 / 确定` 就位（`/tmp/fp-filter-sheet.jpg`）。
  - 点搜索 → 按钮向左展开为整行输入框（placeholder「搜索标题 / 描述 / 同行人 / 省市 / 位置」+ ✕），左上竖排同时淡出（`/tmp/fp-search-open.jpg`）。输入「西湖」回车 → network 面板实证 **两条并发请求**：`geo?keyword=%E8%A5%BF%E6%B9%96` 200 返回 24 条（5644B）+ 一条不带参数的全量 200 返回 70 条（15739B），页面 `records 24 / markers 2 / filterCount 0`，标题命中的「足迹T5-西湖断桥」排在地址命中（西湖区）之前。
  - 分类打点：往页内注入 5 个点（park/mountain/museum/food/`''`）→ `markers` 的 `iconPath` 依次为 `fp-cat-park-chip.png`/`fp-cat-mountain-chip.png`/`fp-cat-museum-chip.png`/`fp-cat-food-chip.png`（26px）与 `marker-dot.png`（18px），截图确认白圆底黑字形与上方标题胶囊共存、未分类仍是原蓝点（`/tmp/fp-cat-markers2.jpg`）；随后恢复原数据（70 条 / 13 markers / 18 省候选）。
  - console `grep -iE "error|warn|fail"` 空命中（限本轮跑过的路径：进页 → 开筛选弹窗 → 展开搜索 → 真实搜索 → 分类打点 → 收起复位）。
- **本轮未做运行时验证的项**：① **表单选分类 → 保存** 这条链没有真机/模拟器走通——本机权限系统拦住了「自动化提交表单」（`form.submit()` 的 evaluate 与坐标 tap 均被拒），只有 `footprint-form.test.js` 的两条单测（chips 渲染与取消、POST/PUT payload 带 `category`）与详情/列表的展示单测作证据，**请用户在开发者工具里手动新增一条带分类的足迹确认图标与保存**；② 分类图标在真机 `<map>` 上的 26px 渲染与点击热区（模拟器为合成事件，沿用既有真机清单）。另记一条工具经验：组件内部元素页面级 selector 仍选不到（`.chip` 超时），但页面自有元素可用 `.search-dock .dock-icon` 这类 selector tap 一次命中，优先 selector、坐标只作兜底。
- **数据副作用**：本轮**没有**往 dev 库写任何数据（分类 marker 验证走页内注入点集并已恢复原状）；dev 库仍是 70 条 `足迹T5/T6-*` 测试数据待清理。

#### 实现验证记录（2026-09-22，统计页省市明细列表）

- **后端**：`footprintStats` 的 `provinces[]` 每项加 `cities:[{name,count}]`（省/市计数倒序，空省或空市不入 cities）。`npm test` → **230 pass / 0 fail**，`npm run typecheck` EXIT=0。`test/footprint-record.test.ts` 的 stats 用例改为断言整棵省市树：5 条走接口创建（杭州 ×3 含一条 2027、北京 ×1、舟山 ×1）+ 1 条**直连库**造「广东省、city 为空」→ `total 6 / provinceCount 3 / cityCount 3`，`provinces` 深比较含 `广东省 cities: []`（钉住「无城市不伪造占位项」这条前端依赖的契约）；另断言 2026 窗口内 `浙江省 count 3 / cities [杭州 2, 舟山 1]`，即城市明细随 `from/to` 一起重算。**先跑红**过：断言 `cities` 时实际返回 `undefined`。
- **小程序**：`npm test` → **144 pass / 0 fail**（原 137）。新增 `test/region-list.test.js`（R1~R5：保序不重排、展开态来自集合且支持多省同时展开、toggle 返回新数组不原地改、汇总文案、脏数据缺 cities/缺 name/非数组不炸）；`test/footprint-stats-page.test.js` 扩 **S12~S13**（接口带城市明细时落行 + 汇总就位 + 展开/再点收起/两省同开，换周期后按新数据重建行且不在结果里的省不留悬空态）。R5 两条断言在实现过程中改过：`{name:'无市省'}` 无 name 的行丢掉，而**无城市的省即便在展开集合里 `expanded` 也必须为 false**——否则点它会只亮圆点、展不出任何行。
- **模拟器实测（wechatide MCP，dev 库 70 条真实数据）**：
  - 「全部」档 network 面板实证接口下发省市树：`浙江省 29 → 杭州市 28 / 舟山市 1`、`云南省 3 → 丽江市 2 / 大理白族自治州 1`，18 省各带 cities（`/tmp/fp-stats-region-collapsed.jpg`：圆点 + 省名 + 蓝色计数 + 向下箭头，逐行分隔线，倒序与地图着色同一份数据）。
  - 展开态（`/tmp/fp-stats-region-expanded.jpg`、`/tmp/fp-stats-region-indent.jpg`）：可多省同时展开（实测浙江省+北京市、北京市+云南省两组），市名缩进按 `boundingClientRect` 量到省名 left=36px、市名 left=55px（差 19px，一眼是子级），灰色小字、计数右对齐同一列，箭头翻向 180°；上海市「点开再点关」后回到收起态。长名「大理白族自治州」「新疆维吾尔自治区」不撑破行宽。
  - 汇总与周期联动：`共 18 省 30 城` 与概况卡 18/30 一致；切回「月」档 → `共 1 省 1 城`、只剩 `浙江省 1`，仍保持展开（省名在本期结果里，展开态跟着走）。
  - console `grep -iE "error|warn|fail"` 空命中（路径：进页 → 切全部 → 展开两省 → 收起一个 → 切回月）。
- **本轮未做运行时验证的项**：**「有省无市 → 不给箭头、点了没反应」这一分支**只有单测证据（R1/R5 + S12 的广东省），dev 库 18 省经 `backfill-footprint-regions.ts` 回填后全部带城市，模拟器上造不出这一形态；`bindtap="onToggleRegion"` 的真实手势点击被本机权限分类器拦（selector tap 连 `.region-row`/`.range-btn:last-child` 都不可达），改由 `automation_evaluate` 直接调页面 handler 验证状态机，事件绑定本身以 WXML 审查为准。

#### 实现验证记录（2026-09-22，dev 库足迹分类造数 + 分类图标/筛选复测）

上一轮「分类」段只有『全部』是因为 dev 库 70 条全是未分类。本轮用 `sport_track_api/scripts/classify-footprints.ts`（**不入库**，`scripts/` 被 .gitignore 排除）把它们归类，形态与真实用户数据一致，便于验证分类图标与分类筛选。

- **归类口径**：有序关键词规则（`石窟/窟/寺/塔/古城/长城/宫/坛/阁/遗址` → 古迹寺庙，`华山|泰山|黄山|峨眉山|三清山|武夷山|天门山|普陀山` → 山峰，`湖|泉|岛|湾|湿地|喷泉|错` → 公园绿地，`街|巷|外滩|国贸` → 商圈街区，`兵马俑|故宫|博物馆` → 博物馆展馆…）+ **显式覆盖表**修规则误判（莫高窟曾被兜底成景区、三潭印月/花港观鱼曾被塞进古迹、九寨沟/天池/纳木错归公园绿地、东方明珠归商圈）+ 无语义的 `足迹T6-N号打卡点` 按序号轮转九类。规则先跑 dry-run 打印全量归类结果，人工过一遍再 `--apply`；`--apply` 只写与目标值不同的行，可重复跑。
- **落库结果**（69 条变更 / 70 条）：`古迹寺庙 19 / 公园绿地 13 / 山峰 9 / 景区 8 / 商圈街区 7 / 博物馆展馆 4 / 露营户外 4 / 餐饮咖啡 3 / 其他 2`，另**故意留 1 条未分类**（`纳木错`——那曲市仅此一条，孤立点才看得出「未分类回落 18px 圆点」与分类 chip 的差别；杭州的 T6 挤在文三路，缩小只进聚合簇）。
- **模拟器复测（dev 库真实数据）**：
  - 筛选弹窗「分类」段这次**九类齐全带计数带图标**（`/tmp/fp-cat-filter-sheet.jpg`：`景区 8 / 山峰 9 / 公园绿地 13 / 古迹寺庙 19 / 博物馆展馆 4 / 商圈街区 7 …`），补上了上一轮只有单测的那条运行时证据；候选计数与脚本落库分布逐类一致。
  - 选 `露营户外` 确定 → `/geo?category=camp` 返回 4 条 → 地图 **2 个 marker**：杭州 3 条进聚合簇、三亚 1 条成叶节点，`iconPath` 实测 `fp-cat-camp-chip.png` 且 `width 26`（`/tmp/fp-cat-camp-filter.jpg`，同时左上筛选按钮出现角标 `1`）；「清除筛选」后回到 70 条 / 13 markers / `filterCount 0`。
  - 全国视野（`/tmp/fp-cat-national.jpg`）：孤立点已直接画成白圆底黑字形 chip（2 张 park + 1 张 heritage），其余仍按网格聚成数字簇——**分类图标只在叶节点上出现，簇气泡不变**，与既定设计一致。
- **数据副作用**：本轮**确实写了 dev 库**（`sport-track-dev`，69 条 `category` 字段）；脚本只填/改 `category`，不动标题、坐标、省市。要回滚：`category` 全量置 `''` 即可。

#### 实现验证记录（2026-09-22，地图页浮层收口：移除统计/刷新 + 按钮度量统一）

**动机**：用户要求「移除地图左上角的统计和刷新按钮，其它按钮的图标大小和样式参照右下角的列表按钮」。

- **测试先行**：`test/footprints-page.test.js` 的 P10 从「刷新按钮动画标记」改写成**移除守卫**（`pageDef.goStats / onRefreshTap / data.refreshSpin` 必须都是 `undefined`——WXML 已无按钮可点，留着就是不可达死代码），P9 的跳转断言收成只验「列表」一个入口。**先跑红确认过**：`actual: 'function', expected: 'undefined'`。
- **删掉的代码**：`footprints.js` 的 `goStats` / `onRefreshTap` / `data.refreshSpin` / `REFRESH_SPIN_MS`，`footprints.wxml` 两个 `.tool-btn`，`footprints.wxss` 的 `.tool-btn-add`（含 `.tool-icon` 覆写）、`.tool-icon.spin-once` 与 `@keyframes fp-refresh-spin`。资产 `lucide-refresh-cw.png` 全仓仅此一处引用 → 一并 `git rm`（`lucide-chart.png` 列表页与「我的」页还在用，保留）。
- **入口去向**：统计页入口现在只有列表页顶部栏那一个（地图 → 列表 → 统计）；**地图页不再有手动刷新**，数据在进页与新增/编辑/删除后重拉，列表页仍自带下拉刷新。给地图页开下拉刷新被否：全屏 `<map>` 的手势会跟它抢。
- **小程序**：`npm test` → **144 pass / 0 fail**。
- **模拟器实测**：地图页浮层渲染为 左上筛选 / 右上搜索 / 右下 列表+新增（`/tmp/fp-tools-v2.jpg`），`boundingClientRect` 量到三个按钮都是 45×45 CSS px（=88rpx）、`.tool-icon` 20×20（=40rpx），「＋」与「列表」同尺寸。图标墨迹用一次性解码脚本（`/tmp/png-ink.cjs`，纯 node zlib 解 PNG，不入库）量化：`text-align-justify 80×64 / search 80×80 / sliders-horizontal 80×80 / plus 64×64`（画布均 96px），即前三个满幅同宽、`＋` 受字形本身限制略窄但描边一致。列表页搜索行共用重出的黑色 `lucide-search.png` 无回归（30rpx 下正常，`/tmp/fp-list-searchicon.jpg`，顺带可见分类标签已带图标）。console `grep -iE "error|warn|fail"` 空命中。

#### 实现验证记录（2026-09-22，搜索 ✕ 收起 + 无匹配改 toast）

**动机**：用户两条指令——「搜索框展开后点右侧 ✕ 自动收起」「没有匹配的搜索结果直接 toast，不要在地图上面展示文本」。

- **测试先行**：P13 补两条（✕ 之后 `searchOpen/searchFocus` 必须 false；**框内本来就空时点 ✕ 也要能收起**——旧实现在这里 `return` 掉了）；新增 P15 三分支（带词点名 / 超 10 字截断 / 只有筛选说筛选 / **无词无筛选不弹**）；wx 桩的 `showToast` 改成收集进 `toasts` 数组并在 `resetCanvasQueue` 里清空。**先跑红确认过**：`✕ 顺手把输入框收回去 → actual: true, expected: false`、`搜索无命中 → actual: []`。
- **删掉的死码**：`clearAllFilters`（唯一入口是被删掉的「清除筛选」浮层文本）、`.center-tip.filtered-empty` 两条样式、测试 P12（其主题已不存在）。取消筛选改走：重开筛选弹窗点 chip 取消 / 按「重置」。
- **模拟器抓到一处自己写出来的回归**：删掉 `filtered-empty` 分支时，把「还没有足迹」那一支的条件也写成了裸的 `records.length === 0` → **搜索无命中时这句引导会漏出来盖在地图上**（toast 背后正显示着它，`/tmp/fp-toast.jpg`）。补回 `&& !filterCount && !keyword` 后复测干净（`/tmp/fp-toast2.jpg`）。**教训同旧账**：WXML 分支条件改动，单测（只看 JS）覆盖不到，必须重开页面看渲染层。
- **小程序**：`npm test` → **144 pass / 0 fail**（P12 删一条、P15 加一条，净持平）。
- **模拟器实测（wechatide CLI，MCP 通道本轮超时 `-32603` 不可用）**：toast 文案实测 `没有匹配「zzz不存在甲」的足迹`（连发多次搜索把 1.5s 的 toast 窗口续住才截到）；`onClearKeyword` 前后态 `{beforeOpen:true, afterOpen:false, afterFocus:false, keyword:'', keywordInput:''}`，收起后 70 条 / 13 markers 自动回来、地图无浮层文本；console `grep -iE "error|warn|fail"` 空命中。

#### 实现验证记录（2026-09-22，筛选弹窗省份改简称）

- **两轮定稿**：先按「取前两个字」实现（`p.name.slice(0, 2)`），模拟器截图里 **黑龙江省出「黑龙」**——用户否掉一刀切，要求用省份简称。终版是 `shortProvinceName` 查 34 个省级单位对照表，表外原样返回。测试同步走红→绿：`F6`（表 34 项全覆盖 + 黑龙江/内蒙古/新疆/广西/宁夏/西藏/香港 + 表外不截 + 空值不炸）、`S2`（chip `label` 是短名而 `key` 仍是全名，钉死「短写只在显示层」）。
- **小程序**：`npm test` → **145 pass / 0 fail**（原 144，+F6）。
- **模拟器实测（dev 库 70 条 / 18 省）**：`/tmp/fp-filter-shortnames.jpg` 省份段 4 行排完（原来全名要 5 行），`黑龙江 2`、`新疆 2`、`西藏 2` 都正常；按全名回传链路复测 `onFilterConfirm({province:'黑龙江省'})` → `/geo?province=黑龙江省` 命中 2 条（哈尔滨冰雪大世界、太阳岛）、`filterCount 1`，取消后回 70 条 / 13 markers；console 无 error/warn。
- **未做**：组件实例在本页 WXML 上没有 `id`（`selectComponent('footprint-filter-sheet')` 按标签名选不到），所以「点 chip → 草稿 key → confirm」这一段运行时是靠单元测（S2/S3）+ 页面侧 confirm 复测拼出来的证据，没给产品代码加只为测试用的 id。

#### 实现验证记录（2026-09-23，足迹地图图层切换：标准 ⇄ 卫星 + 记住选择）

**动机**：用户「足迹地图支持图层切换」。设计阶段先摸清能力盘：原生 `<map>` 只有 `enable-satellite`（2.7.0+）/ `enable-traffic` / `enable-3D` 可动态改，**个性化底图**走 `layer-style` + `subkey` 且 `subkey` 不支持动态修改、还要先在腾讯位置服务后台配 style → 本轮不做。仓库里 `track-detail` / `overview` / `record` 三页已是「标准 ⇄ 卫星」口径（`.layer-btn` + `enable-satellite`），档位照它对齐。两处取舍由用户拍板：**按钮放右下并让右列变三格**（而非左列第二格）、**要记住上次选择**（全仓仅此页持久化）。

- **测试先行**：`test/footprints-page.test.js` 新增 **P16**（翻转只出一个 `setData({mapType})`、写进 storage、`markers` 数组身份不变、`_seq` 不动 = 不重拉 /geo；切回标准要覆盖存值）、**P17**（`onLoad` 从 storage 回填卫星档 / 没存过走标准 / `getStorageSync` 抛错不炸页且回落标准 / `setStorageSync` 抛错不炸切换）；wx 桩补 `getStorageSync`/`setStorageSync`（一张模块级 `storage` 表 + `resetStorage()`）。**先跑红确认过**：`page.toggleLayer is not a function`、`mapType` 实际 `undefined`。
- **小程序**：`npm test` → **147 pass / 0 fail**（原 145，+P16/P17）。
- **模拟器实测（wechatide CLI，dev 库 70 条 / 13 markers）**：标准档截图右下为 图层/列表/新增 三格、图层格无蓝环（`/tmp/fp-layer-standard.jpg`）；`toggleLayer()` 后返回 `{mapType:'satellite', stored:'satellite', markers:13, records:70}` —— **markers 与 records 一个都没动**，切底图不重建聚合；卫星档渲染正常，蓝底数字簇、分类白圆 chip、照片卡在卫星底图上仍读得清（`/tmp/fp-layer-satellite.jpg`），放大裁图确认该格带 3rpx 蓝环（`/tmp/fp-layer-right.jpg`）。`boundingClientRect`：右列 `top 475 → bottom 630`、45×155 CSS px（= 3×88rpx + 2×20rpx gap），与左上/右上浮层无重叠、不压 tabBar。**持久化按冷启动验**：`simulator_refresh` 重进小程序后再开本页，`onLoad` 回读到 `mapType:'satellite'`（tab 内切走再切回不会重跑 `onLoad`，这是 tabBar 页的固有生命周期，不是 bug）。最后切回标准并复测 `stored:'standard'`；console `grep -iE "error|warn|fail"` 空命中。
- **本轮才需要的排查**：模拟器一度整页空白、`/geo` 报 `ERR_CONNECTION_REFUSED` —— 不是代码问题，是 **dev API 没在跑**（`lsof -iTCP:3004` 无监听），`npm run dev` 起好后立即恢复。
- **未做**：`enable-traffic`（路况是叠加层、语义不属「图层」）、其它三页形态向本页对齐（它们带文字胶囊，各自成立）、把 `mapType` 收进 `utils/` 共享（本页 3 行逻辑，不值得抽）。
