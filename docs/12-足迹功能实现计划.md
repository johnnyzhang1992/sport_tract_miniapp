# 足迹功能（Footprint Records）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 小程序新增「足迹」tab，用户可创建/查看/编辑/删除自己去过的地方（时间、地点、人物、标题、描述、≤3 图），地图用原生 marker 聚合展示，点击弹详情。

**Architecture:** 后端 Fastify+Mongoose 新增 `FootprintRecord` 模型 + `/footprint-records` CRUD 路由（抄 activities 的 findOwned/防刷/OSS 签名模式）+ `/geo/search` 腾讯 place 代理（缓存+限流）；小程序主包新增 tab 页 `pages/footprints`（地图⇄列表 + 详情弹窗），新分包 `packageFootRecords` 放编辑页与自建地图选点页。与现有点亮地图（packageFootprint、`/stats/footprint`）完全独立。

**Tech Stack:** Fastify 5 / Mongoose 9 / zod 4 / node:test + app.inject；原生微信小程序（`<map>` joinCluster、wx.chooseLocation、wx.compressImage、OSS 签名直传）。

**Spec:** `/Users/johnnyzhang/extra_sport/sport_track_miniapp/docs/11-足迹功能设计.md`（实现前先通读；本计划与其冲突处以 spec 为准并停下来报告）

## Global Constraints

- 接口前缀 `/sport-track/api`；响应包裹 `{ success, code, message, data }`，一律 `return success(data, message)`。
- 所有 `footprint-records` 接口 `onRequest: [fastify.authenticate]`；查询/变更强制带 `userId` 条件，非本人一律 404「足迹不存在」。
- 命名避让：不得占用 `footprint` 前缀的既有名字（`/stats/footprint`、`user.footprintCache`、`packageFootprint`）。新 collection 为 `footprintrecords`（model 名 `FootprintRecord`）。
- 图片：最多 3 张；OSS 存裸 URL（入库前 `cleanUrl`，出库 `getSignedUrl`）；上传前 `wx.compressImage(quality:80)`（失败降级原图）。
- 腾讯额度：除 `/geo/search` 外不得新增任何 WebService 调用；`/geo/search` 必须走进程内缓存 + 每用户 10 次/分钟限流。
- 测试命令（API 仓库）：单文件 `WX_MOCK_LOGIN=true node --import tsx --test --test-concurrency=1 test/<file>.test.ts`；提交前 `npm run typecheck` 必须通过。测试连本地 Mongo（`mongodb://localhost:27017/sport-track-dev`），**清理数据只删本次测试用户/标题，禁止 deleteMany 全表**。
- 小程序 UI 验证：用 wechatide MCP 控制开发者工具编译+模拟器截图验证（既有节奏：方案→实现→截图微调）；不许闪屏、加载有骨架/菊花态。
- git：**只 commit 不 push**（用户测试通过后再推）；两仓库分开提交；commit message 用 `feat:`/`test:` 前缀+中文描述（对齐 `git log` 风格）。
- 分支：两仓库各自 `git checkout -b feat/footprint-records`（Task 1 Step 0 建，miniapp 现有未提交改动会带入分支属正常，不得丢弃、不得混入本功能提交）。

## 文件结构

**sport_track_api**（新增/修改）：
- Create `src/models/footprint-record.model.ts` — schema + 索引
- Create `src/services/footprint-record.ts` — CRUD/列表/geo 轻量/归属/防刷/OSS 清理
- Create `src/routes/footprint-record.routes.ts` — 6 个端点
- Modify `src/utils/validators.ts` — 追加 3 个 zod schema
- Modify `src/app.ts` — 注册路由
- Modify `src/services/geo.ts` — `searchPlaces`（缓存 + 注入式 fetcher）
- Modify `src/routes/geo.routes.ts` — `GET /search` + `assertSearchQuota`
- Create `test/footprint-record.test.ts`、`test/geo-search.test.ts`

**sport_track_miniapp**（新增/修改）：
- Modify `miniprogram/app.json` — 第 4 个 tab、分包 `packageFootRecords`、`requiredPrivateInfos` 加 `chooseLocation`
- Create `miniprogram/assets/icons/tab-footprint-gray.png` / `tab-footprint-blue.png`
- Create `miniprogram/pages/footprints/footprints.{js,wxml,wxss,json}` — tab 主页（地图⇄列表、详情弹窗、FAB）
- Create `miniprogram/packageFootRecords/pages/record-edit/record-edit.{js,wxml,wxss,json}`
- Create `miniprogram/packageFootRecords/pages/map-picker/map-picker.{js,wxml,wxss,json}`

---

### Task 1: 分支 + API 模型与校验（FootprintRecord / zod）

**Files:**
- Create: `sport_track_api/src/models/footprint-record.model.ts`
- Modify: `sport_track_api/src/utils/validators.ts`（文件末尾 export type 区之前追加）
- Test: `sport_track_api/test/footprint-record.test.ts`（本任务只写模型/校验部分，Task 2 续写接口部分）

**Interfaces:**
- Produces: `FootprintRecordModel`（doc 字段：`userId, visitDate, title, people[], description, location{name,address,province,city,adcode,latitude,longitude}, photos[], createdAt, updatedAt`）；`CreateFootprintRecordSchema` / `UpdateFootprintRecordSchema` / `ListFootprintQuery`（zod，输入类型 `CreateFootprintRecordInput`）。

- [ ] **Step 0: 建分支（两仓库）**

```bash
cd /Users/johnnyzhang/extra_sport/sport_track_api && git checkout -b feat/footprint-records
cd /Users/johnnyzhang/extra_sport/sport_track_miniapp && git checkout -b feat/footprint-records
```
若 miniapp 未提交改动导致 checkout 报错，停下来问用户，不得 stash/丢弃。

- [ ] **Step 1: 写失败测试 `test/footprint-record.test.ts`**

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { Types } from 'mongoose';
import { FootprintRecordModel } from '../src/models/footprint-record.model.js';
import { CreateFootprintRecordSchema } from '../src/utils/validators.js';

const baseDoc = {
  userId: new Types.ObjectId(),
  visitDate: '2024-05-01',
  title: '足迹测试-模型',
  location: { latitude: 30.24, longitude: 120.15 },
};

// 模型层只验必填与默认值；日期格式在 zod 层校验（模型存任意日期字符串合法）
test('模型：合法文档可保存，默认值生效，必填缺失被拒绝', async () => {
  const doc = await FootprintRecordModel.create(baseDoc);
  assert.equal(doc.title, '足迹测试-模型');
  assert.deepEqual(doc.photos, []);
  assert.deepEqual(doc.people, []);
  assert.equal(doc.description, '');
  assert.equal(doc.location.province, ''); // 省市由服务层补，模型默认空串
  await assert.rejects(FootprintRecordModel.create({ ...baseDoc, title: undefined }));
  await assert.rejects(FootprintRecordModel.create({ ...baseDoc, location: { latitude: 1 } })); // 缺 longitude
  await FootprintRecordModel.deleteOne({ _id: doc._id });
});


test('zod：visitDate 必须 YYYY-MM-DD；people≤10 每项≤20 字；photos≤3；标题 1-50', () => {
  const ok = CreateFootprintRecordSchema.parse({
    visitDate: '2024-05-01', title: 'a'.repeat(50),
    people: ['张三', '李四'], description: 'x',
    location: { name: '西湖', address: '杭州市西湖区', latitude: 30.24, longitude: 120.15 },
    photos: [],
  });
  assert.equal(ok.people.length, 2);
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, visitDate: '2024-5-1' }));
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, title: '' }));
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, title: 'a'.repeat(51) }));
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, people: Array(11).fill('甲') }));
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, people: ['a'.repeat(21)] }));
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, photos: ['u?1', 'u?2', 'u?3', 'u?4'].map((s) => `https://e.com/${s}.jpg`) }));
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, description: 'x'.repeat(501) }));
  assert.throws(() => CreateFootprintRecordSchema.parse({ ...ok, location: { ...ok.location, latitude: 91 } }));
});
```


- [ ] **Step 2: 跑测试确认失败**

Run: `cd sport_track_api && WX_MOCK_LOGIN=true node --import tsx --test --test-concurrency=1 test/footprint-record.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 写模型 `src/models/footprint-record.model.ts`**

```ts
import mongoose from 'mongoose';

const { Schema, model, models } = mongoose;

const locationSchema = new Schema(
  {
    name: { type: String, default: '' }, // 地点名（POI 名或用户自填）
    address: { type: String, default: '' }, // 详细地址
    province: { type: String, default: '' }, // 服务端离线 locateRegion 补全
    city: { type: String, default: '' },
    adcode: { type: Number, default: 0 },
    latitude: { type: Number, required: true },
    longitude: { type: Number, required: true },
  },
  { _id: false },
);

const footprintRecordSchema = new Schema(
  {
    userId: { type: Schema.Types.ObjectId, ref: 'User', required: true },
    visitDate: { type: String, required: true }, // YYYY-MM-DD，字符串免时区、可排序
    title: { type: String, required: true },
    people: { type: [String], default: [] },
    description: { type: String, default: '' },
    location: { type: locationSchema, required: true },
    photos: { type: [String], default: [] }, // 裸 OSS URL（上限 3，zod 层约束），读时签名
  },
  { timestamps: true, versionKey: false },
);

footprintRecordSchema.index({ userId: 1, visitDate: -1 });

export type FootprintRecord = mongoose.InferSchemaType<typeof footprintRecordSchema>;

export const FootprintRecordModel =
  models.FootprintRecord ?? model('FootprintRecord', footprintRecordSchema);
```

- [ ] **Step 4: validators.ts 追加（在 `// ==================== 运动记录（M2） ====================` 风格的分节注释后，export type 区之前）**

```ts
// ==================== 足迹记录（footprint-records） ====================

const VisitDateSchema = z
  .string()
  .regex(/^\d{4}-\d{2}-\d{2}$/, '日期格式应为 YYYY-MM-DD');

/** 照片 URL（裸地址或带签名参数均可，服务层 cleanUrl 归一） */
const FootprintPhotoSchema = z.string().url('照片地址不合法').max(600);

/** 创建/编辑足迹（编辑为整体替换，两 schema 相同） */
export const CreateFootprintRecordSchema = z.object({
  visitDate: VisitDateSchema,
  title: z.string().trim().min(1, '标题不能为空').max(50, '标题最长 50 字'),
  people: z
    .array(z.string().trim().min(1, '人名不能为空').max(20, '人名最长 20 字'))
    .max(10, '人物最多 10 个')
    .default([]),
  description: z.string().max(500, '描述最长 500 字').optional().default(''),
  location: z.object({
    name: z.string().max(100).optional().default(''),
    address: z.string().max(200).optional().default(''),
    latitude: z.number().min(-90, '纬度不合法').max(90, '纬度不合法'),
    longitude: z.number().min(-180, '经度不合法').max(180, '经度不合法'),
  }),
  photos: z.array(FootprintPhotoSchema).max(3, '每条足迹最多 3 张图片').optional(),
});

export const UpdateFootprintRecordSchema = CreateFootprintRecordSchema;

/** 足迹列表查询 */
export const ListFootprintQuery = z.object({
  keyword: z.string().trim().max(50).optional(),
  page: z.coerce.number().int().positive().default(1),
  pageSize: z.coerce.number().int().positive().max(100).default(20),
});
```

并在文件底部 type 导出区追加：

```ts
export type CreateFootprintRecordInput = z.infer<typeof CreateFootprintRecordSchema>;
export type ListFootprintQueryInput = z.infer<typeof ListFootprintQuery>;
```

- [ ] **Step 5: 跑测试确认通过 + typecheck**

Run: `WX_MOCK_LOGIN=true node --import tsx --test --test-concurrency=1 test/footprint-record.test.ts && npm run typecheck`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/models/footprint-record.model.ts src/utils/validators.ts test/footprint-record.test.ts
git commit -m "feat: 足迹记录模型 FootprintRecord + zod 校验（独立于轨迹点亮 footprint）"
```

---

### Task 2: API 服务层 + 路由（CRUD / geo / 防刷 / OSS 清理）+ 集成测试

**Files:**
- Create: `src/services/footprint-record.ts`
- Create: `src/routes/footprint-record.routes.ts`
- Modify: `src/app.ts:17`（import 区加一行）与 `src/app.ts:83`（adminRoutes 注册前加注册）
- Test: 续写 `test/footprint-record.test.ts`

**Interfaces:**
- Consumes: Task 1 的 `FootprintRecordModel`、`CreateFootprintRecordSchema`、`ListFootprintQuery`；`src/services/oss.ts` 的 `getSignedUrl/cleanUrl/deleteOssObjects`；`src/services/region.ts` 的 `locateRegion(lat,lng): {province,city}|null`；`src/utils/response.ts` 的 `success`；`src/utils/app-error.ts` 的 `AppError`。
- Produces（路由挂载于 `/sport-track/api/footprint-records`，DTO 均为 lean doc 映射）：
  - `FootprintDto = { id, visitDate, title, people, description, location:{name,address,province,city,adcode,latitude,longitude}, photos(签名URL), createdAt, updatedAt }`
  - `GET /footprint-records/geo` → `{ items: [{ id, title, visitDate, latitude, longitude, coverPhoto }] }`
  - `GET /footprint-records` → `{ items: FootprintDto[], total, page, pageSize }`
  - `POST /` body=`CreateFootprintRecordInput` → `{ record: FootprintDto }`
  - `PUT /:id` 同上 → `{ record: FootprintDto }`；`DELETE /:id` → `data:null`；`GET /:id` → `FootprintDto`

- [ ] **Step 1: 写失败的集成测试（追加到 `test/footprint-record.test.ts` 顶部补齐 import，主体如下）**

```ts
// 追加 import：test、before、after；buildApp、FastifyInstance、mongoose、api 前缀
let app: FastifyInstance;
let tokenA = '';
let tokenB = '';
const userOids: unknown[] = [];

async function loginAs(code: string): Promise<string> {
  const res = await app.inject({ method: 'POST', url: '/sport-track/api/auth/login', payload: { code } });
  assert.equal(res.statusCode, 200, res.body);
  const t = res.json().data.accessToken as string;
  // 记录 oid 供清理：mock 登录同一 code 幂等返回同一用户
  return t;
}
function req(method: string, url: string, token: string, payload?: unknown) {
  return app.inject({ method, url: `/sport-track/api/footprint-records${url}`, headers: { authorization: `Bearer ${token}` }, payload });
}
const fp = (over: Record<string, unknown> = {}) => ({
  visitDate: '2024-05-01', title: '足迹测试A',
  location: { name: '西湖', address: '杭州市西湖区', latitude: 30.24, longitude: 120.15 },
  ...over,
});

before(async () => {
  app = await buildApp({ logger: false });
  await app.ready();
  tokenA = await loginAs('fp-user-a');
  tokenB = await loginAs('fp-user-b');
  await FootprintRecordModel.deleteMany({ title: /^足迹测试/ });
});
after(async () => {
  await FootprintRecordModel.deleteMany({ title: /^足迹测试/ });
  await app.close();
  await mongoose.disconnect().catch(() => {});
});

test('CRUD：创建→列表→详情→更新→删除', async () => {
  const created = await req('POST', '', tokenA, fp({ people: ['张三'], description: '春天', photos: ['https://example.com/a.jpg'] }));
  assert.equal(created.statusCode, 200, created.body);
  const id = created.json().data.record.id as string;
  assert.equal(created.json().data.record.photos[0], 'https://example.com/a.jpg'); // OSS 未配置时 getSignedUrl 原样返回

  const list = await req('GET', '?page=1&pageSize=20', tokenA);
  assert.ok(list.json().data.items.some((r: any) => r.id === id));

  const kw = await req('GET', '?keyword=西湖', tokenA);
  assert.equal(kw.json().data.items.length, 1);
  const kwMiss = await req('GET', '?keyword=不存在词xyz', tokenA);
  assert.equal(kwMiss.json().data.items.length, 0);

  const upd = await req('PUT', `/${id}`, tokenA, fp({ title: '足迹测试A-更新', people: [] }));
  assert.equal(upd.statusCode, 200, upd.body);
  assert.equal(upd.json().data.record.title, '足迹测试A-更新');

  const del = await req('DELETE', `/${id}`, tokenA);
  assert.equal(del.statusCode, 200);
  const gone = await req('GET', `/${id}`, tokenA);
  assert.equal(gone.statusCode, 404);
});

test('归属隔离：B 不可读写 A 的记录（404）；未登录 401', async () => {
  const id = (await req('POST', '', tokenA, fp({ title: '足迹测试B' }))).json().data.record.id as string;
  assert.equal((await req('GET', `/${id}`, tokenB)).statusCode === 200, false);
  assert.equal((await req('GET', `/${id}`, tokenB)).json().code, 404);
  assert.equal((await req('PUT', `/${id}`, tokenB, fp())).json().code, 404);
  assert.equal((await req('DELETE', `/${id}`, tokenB)).json().code, 404);
  assert.equal((await app.inject({ method: 'GET', url: '/sport-track/api/footprint-records' })).statusCode, 401);
  await req('DELETE', `/${id}`, tokenA);
});

test('校验与排序：坏日期 400；列表按 visitDate 倒序同日按创建倒序', async () => {
  const bad = await req('POST', '', tokenA, fp({ visitDate: '2024-5-1', title: '足迹测试C' }));
  assert.equal(bad.statusCode, 400);
  await req('POST', '', tokenA, fp({ visitDate: '2023-01-01', title: '足迹测试C-旧' }));
  const r1 = (await req('POST', '', tokenA, fp({ visitDate: '2024-06-01', title: '足迹测试C-新1' }))).json().data.record.id;
  const r2 = (await req('POST', '', tokenA, fp({ visitDate: '2024-06-01', title: '足迹测试C-新2' }))).json().data.record.id;
  const items = (await req('GET', '?keyword=足迹测试C', tokenA)).json().data.items;
  assert.equal(items[0].id, r2); // 同日：后创建的在前
  assert.equal(items[1].id, r1);
  assert.equal(items[2].visitDate, '2023-01-01'); // 跨日：visitDate 倒序
  await FootprintRecordModel.deleteMany({ title: /^足迹测试C/ });
});

test('geo 端点：轻量字段 + coverPhoto，不分页', async () => {
  await req('POST', '', tokenA, fp({ title: '足迹测试D', photos: ['https://example.com/d.jpg'] }));
  const items = (await req('GET', '/geo', tokenA)).json().data.items;
  const d = items.find((r: any) => r.title === '足迹测试D');
  assert.ok(d);
  assert.equal(d.coverPhoto, 'https://example.com/d.jpg');
  assert.equal('description' in d, false);
  await FootprintRecordModel.deleteMany({ title: '足迹测试D' });
});

test('防刷：1 小时第 31 条创建返回 429', async () => {
  // 专属用户，避免污染其他用例计数
  const tokenC = await loginAs('fp-user-rate');
  await FootprintRecordModel.deleteMany({ title: /^足迹测试E/ }); // 重跑残留清零
  for (let i = 0; i < 30; i++) {
    const ok = await req('POST', '', tokenC, fp({ title: `足迹测试E${i}` }));
    assert.equal(ok.statusCode, 200, `第 ${i + 1} 条应成功: ${ok.body}`);
  }
  const blocked = await req('POST', '', tokenC, fp({ title: '足迹测试E30' }));
  assert.equal(blocked.statusCode, 429);
  assert.ok(blocked.json().message.includes('30'));
  await FootprintRecordModel.deleteMany({ title: /^足迹测试E/ });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `WX_MOCK_LOGIN=true node --import tsx --test --test-concurrency=1 test/footprint-record.test.ts`
Expected: FAIL（404 路由不存在）

- [ ] **Step 3: 写 `src/services/footprint-record.ts`**

```ts
import { cleanUrl, deleteOssObjects, getSignedUrl } from './oss.js';
import { locateRegion } from './region.js';
import { AppError } from '../utils/app-error.js';
import { FootprintRecordModel } from '../models/footprint-record.model.js';
import type { CreateFootprintRecordInput, ListFootprintQueryInput } from '../utils/validators.js';

/** 足迹新增防刷：1 小时滑动窗口最多 30 条（低于 activities 强度：足迹是静态记录） */
const FP_CREATE_LIMIT = 30;
const FP_CREATE_WINDOW_MS = 3600000;

export async function assertCanCreateFootprint(userId: string) {
  const recent = await FootprintRecordModel.countDocuments({
    userId,
    createdAt: { $gte: new Date(Date.now() - FP_CREATE_WINDOW_MS) },
  });
  if (recent >= FP_CREATE_LIMIT) {
    throw new AppError(429, `创建过于频繁，1 小时内最多新增 ${FP_CREATE_LIMIT} 条足迹`);
  }
}

function escapeRegex(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function toDto(doc: any, sign = true): any {
  return {
    id: String(doc._id),
    visitDate: doc.visitDate,
    title: doc.title,
    people: doc.people ?? [],
    description: doc.description ?? '',
    location: {
      name: doc.location?.name ?? '',
      address: doc.location?.address ?? '',
      province: doc.location?.province ?? '',
      city: doc.location?.city ?? '',
      adcode: doc.location?.adcode ?? 0,
      latitude: doc.location?.latitude,
      longitude: doc.location?.longitude,
    },
    photos: (doc.photos ?? []).map((p: string) => (sign ? getSignedUrl(p) : p)),
    createdAt: doc.createdAt,
    updatedAt: doc.updatedAt,
  };
}

async function findOwnedRecord(id: string, userId: string) {
  const doc = await FootprintRecordModel.findOne({ _id: id, userId }).lean();
  if (!doc) throw new AppError(404, '足迹不存在');
  return doc;
}

function buildLocation(input: CreateFootprintRecordInput) {
  const region = locateRegion(input.location.latitude, input.location.longitude);
  return { ...input.location, province: region?.province ?? '', city: region?.city ?? '' };
}

/** 编辑时清理被移除的旧图（入参已 cleanUrl；签名回传的 URL 归一后才能对上） */
function removedPhotos(oldList: string[], newList: string[]): string[] {
  return oldList.filter((p) => !newList.includes(p));
}

export async function createFootprint(userId: string, input: CreateFootprintRecordInput) {
  const doc = await FootprintRecordModel.create({
    userId,
    visitDate: input.visitDate,
    title: input.title,
    people: input.people,
    description: input.description,
    location: buildLocation(input),
    photos: (input.photos ?? []).map(cleanUrl),
  });
  return toDto(doc);
}

export async function listFootprints(userId: string, query: ListFootprintQueryInput) {
  const filter: Record<string, unknown> = { userId };
  if (query.keyword) {
    const rx = new RegExp(escapeRegex(query.keyword), 'i');
    filter.$or = [
      { title: rx }, { description: rx },
      { 'location.name': rx }, { 'location.address': rx },
    ];
  }
  const [total, docs] = await Promise.all([
    FootprintRecordModel.countDocuments(filter),
    FootprintRecordModel.find(filter)
      .sort({ visitDate: -1, createdAt: -1 })
      .skip((query.page - 1) * query.pageSize)
      .limit(query.pageSize)
      .lean(),
  ]);
  return { items: docs.map((d) => toDto(d)), total, page: query.page, pageSize: query.pageSize };
}

export async function listFootprintGeo(userId: string) {
  const docs = await FootprintRecordModel.find({ userId })
    .sort({ visitDate: -1 })
    .select('title visitDate location.photos location.latitude location.longitude photos')
    .lean();
  return {
    items: docs.map((d) => ({
      id: String(d._id),
      title: d.title,
      visitDate: d.visitDate,
      latitude: d.location?.latitude,
      longitude: d.location?.longitude,
      coverPhoto: d.photos?.[0] ? getSignedUrl(d.photos[0]) : '',
    })),
  };
}

export async function getFootprint(id: string, userId: string) {
  return toDto(await findOwnedRecord(id, userId));
}

export async function updateFootprint(id: string, userId: string, input: CreateFootprintRecordInput) {
  const old = await findOwnedRecord(id, userId);
  const nextPhotos = (input.photos ?? []).map(cleanUrl);
  const doc = await FootprintRecordModel.findOneAndUpdate(
    { _id: id, userId },
    {
      visitDate: input.visitDate,
      title: input.title,
      people: input.people,
      description: input.description,
      location: buildLocation(input),
      photos: nextPhotos,
    },
    { new: true },
  ).lean();
  // 被移除的旧图清理 OSS 文件；失败不阻塞更新（与 markers 一致）
  const stale = removedPhotos(old.photos ?? [], nextPhotos);
  if (stale.length > 0) await deleteOssObjects(stale).catch(() => {});
  return toDto(doc);
}

export async function deleteFootprint(id: string, userId: string) {
  const old = await findOwnedRecord(id, userId);
  await FootprintRecordModel.deleteOne({ _id: id, userId });
  await deleteOssObjects(old.photos ?? []).catch(() => {});
}
```

- [ ] **Step 4: 写 `src/routes/footprint-record.routes.ts`**

```ts
import type { FastifyInstance } from 'fastify';
import { success } from '../utils/response.js';
import {
  CreateFootprintRecordSchema,
  ListFootprintQuery,
  UpdateFootprintRecordSchema,
} from '../utils/validators.js';
import {
  assertCanCreateFootprint,
  createFootprint,
  deleteFootprint,
  getFootprint,
  listFootprintGeo,
  listFootprints,
  updateFootprint,
} from '../services/footprint-record.js';

/**
 * 足迹记录（用户私有的"去过的地方"）
 * 前缀：/api/footprint-records，全部需登录 + 归属过滤
 */
export async function footprintRecordRoutes(fastify: FastifyInstance) {
  fastify.post('/', { onRequest: [fastify.authenticate] }, async (request) => {
    await assertCanCreateFootprint(request.user.userId);
    const input = CreateFootprintRecordSchema.parse(request.body);
    const record = await createFootprint(request.user.userId, input);
    return success({ record }, '足迹已创建');
  });

  fastify.get('/', { onRequest: [fastify.authenticate] }, async (request) => {
    const query = ListFootprintQuery.parse(request.query);
    return success(await listFootprints(request.user.userId, query));
  });

  // 地图专用：全量轻量点（私有数据量级可控，不分页）
  fastify.get('/geo', { onRequest: [fastify.authenticate] }, async (request) => {
    return success(await listFootprintGeo(request.user.userId));
  });

  fastify.get('/:id', { onRequest: [fastify.authenticate] }, async (request) => {
    const { id } = request.params as { id: string };
    return success(await getFootprint(id, request.user.userId));
  });

  fastify.put('/:id', { onRequest: [fastify.authenticate] }, async (request) => {
    const { id } = request.params as { id: string };
    const input = UpdateFootprintRecordSchema.parse(request.body);
    const record = await updateFootprint(id, request.user.userId, input);
    return success({ record }, '足迹已更新');
  });

  fastify.delete('/:id', { onRequest: [fastify.authenticate] }, async (request) => {
    const { id } = request.params as { id: string };
    await deleteFootprint(id, request.user.userId);
    return success(null, '足迹已删除');
  });
}
```

- [ ] **Step 5: 注册路由 `src/app.ts`**

import 区（`topicRoutes` 之后）加：
```ts
import { footprintRecordRoutes } from './routes/footprint-record.routes.js';
```
业务路由区（`topicRoutes` 注册行之后）加：
```ts
await fastify.register(footprintRecordRoutes, { prefix: `${config.apiPrefix}/footprint-records` });
```

- [ ] **Step 6: 跑测试 + typecheck + 全量回归**

Run: `WX_MOCK_LOGIN=true node --import tsx --test --test-concurrency=1 test/footprint-record.test.ts && npm run typecheck && npm test`
Expected: 新用例 PASS，存量用例不回归（全量 test 若本机偶发网络用例失败，确认与本改动无关即可）

- [ ] **Step 7: Commit**

```bash
git add src/services/footprint-record.ts src/routes/footprint-record.routes.ts src/app.ts test/footprint-record.test.ts
git commit -m "feat: 足迹记录 CRUD 接口 /footprint-records（归属过滤/防刷30条时/OSS 清理/geo 轻量端点）"
```

---

### Task 3: `/geo/search` 腾讯 place 代理（缓存 + 限流）

**Files:**
- Modify: `src/services/geo.ts`（文件末尾追加）
- Modify: `src/routes/geo.routes.ts`（先读全文再改，追加分节：route + quota helper）
- Test: Create `test/geo-search.test.ts`

**Interfaces:**
- Consumes: `config.tencentMapKey` / `config.tencentMapReferer`（`src/config/index.ts:49-51`）。
- Produces: `searchPlaces(keyword: string, latitude?: number, longitude?: number): Promise<PlaceItem[]>`，`PlaceItem = { name: string; address: string; latitude: number; longitude: number }`；`GET /sport-track/api/geo/search?keyword&latitude&longitude`（auth，429 限流）；`assertSearchQuota(userId, now?)`。

- [ ] **Step 1: 写失败测试 `test/geo-search.test.ts`**

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { __setPlaceFetcherForTest, __clearPlaceCacheForTest, searchPlaces } from '../src/services/geo.js';

/** 注意：无 TENCENT_MAP_KEY 环境（CI/.env 缺失）时 searchPlaces 直接返回 []，
 *  下面用例对"返回空"与"fetcher 命中"两种情况都容忍，只断言不抛错 + 缓存不重复请求。 */

test('searchPlaces：空 keyword 返回 []；fetcher 不被重复调用（缓存）', async () => {
  let calls = 0;
  __clearPlaceCacheForTest();
  __setPlaceFetcherForTest(async () => {
    calls += 1;
    return { data: { status: 0, data: [{ title: '西湖', address: '杭州市', location: { lat: 30.24, lng: 120.15 } }] } };
  });
  const r1 = await searchPlaces('西湖');
  const r2 = await searchPlaces('西湖');
  if (r1.length === 0) {
    // 无 key 环境：短路返回，fetcher 不应被调用
    assert.equal(calls, 0);
  } else {
    assert.equal(r1[0].name, '西湖');
    assert.equal(calls, 1); // 第二次命中缓存
  }
  assert.deepEqual(r1, r2);
  __setPlaceFetcherForTest(null);
});

test('限流：每用户每分钟 10 次，第 11 次抛 429', async () => {
  const { assertSearchQuota } = await import('../src/services/geo.js');
  const uid = `quota-test-${Date.now()}`;
  const base = Math.floor(Date.now() / 60000);
  for (let i = 0; i < 10; i++) assertSearchQuota(uid);
  assert.throws(() => assertSearchQuota(uid), (e: any) => e.statusCode === 429);
});
```

（route 级只测 401 与 keyword 缺失 400，避免真调腾讯：`app.inject GET /geo/search` 无 token → 401；带 token 无 keyword → 400。token 用 mock 登录，参照 topic.test.ts 的 before 建 app。`assertSearchQuota` 内部按"当前分钟窗口"计数，测试同一分钟内连打 11 次即触发。）

- [ ] **Step 2: 跑测试确认失败**

Run: `WX_MOCK_LOGIN=true node --import tsx --test --test-concurrency=1 test/geo-search.test.ts`
Expected: FAIL（导出不存在）

- [ ] **Step 3: `src/services/geo.ts` 末尾追加**

```ts
// ==================== 地点关键词搜索（腾讯 place/v1/search） ====================

export interface PlaceItem {
  name: string;
  address: string;
  latitude: number;
  longitude: number;
}

type PlaceResponse = { data: { status: number; data?: Array<{ title?: string; address?: string; location?: { lat: number; lng: number } }> } };
type PlaceFetcher = () => Promise<PlaceResponse>;

const PLACE_TTL_MS = 3600000; // 关键词缓存 1 小时：省额度
const PLACE_CACHE_MAX = 500;
const placeCache = new Map<string, { at: number; items: PlaceItem[] }>();

/** 测试注入点：替换上游请求（传 null 还原） */
let placeFetcherOverride: PlaceFetcher | null = null;
export function __setPlaceFetcherForTest(f: PlaceFetcher | null) {
  placeFetcherOverride = f;
}
export function __clearPlaceCacheForTest() {
  placeCache.clear();
}

export async function searchPlaces(keyword: string, latitude?: number, longitude?: number): Promise<PlaceItem[]> {
  const kw = keyword.trim();
  if (!kw || !config.tencentMapKey) return [];
  const cacheKey = `${kw.toLowerCase()}|${latitude != null && longitude != null ? `${latitude.toFixed(2)},${longitude.toFixed(2)}` : ''}`;
  const hit = placeCache.get(cacheKey);
  if (hit && Date.now() - hit.at < PLACE_TTL_MS) return hit.items;

  const fetcher: PlaceFetcher =
    placeFetcherOverride ??
    (async () =>
      axios.get('https://apis.map.qq.com/ws/place/v1/search', {
        params: {
          keyword: kw,
          page_size: 20,
          ...(latitude != null && longitude != null ? { location: `${latitude},${longitude}` } : {}),
          key: config.tencentMapKey,
        },
        headers: config.tencentMapReferer ? { Referer: config.tencentMapReferer } : {},
        timeout: 5000,
      }) as Promise<PlaceResponse>);

  try {
    const res = await fetcher();
    const list = res.data?.status === 0 ? res.data.data ?? [] : [];
    const items: PlaceItem[] = list
      .filter((p) => p.title && p.location)
      .slice(0, 20)
      .map((p) => ({
        name: String(p.title ?? ''),
        address: String(p.address ?? ''),
        latitude: Number(p.location!.lat),
        longitude: Number(p.location!.lng),
      }));
    if (placeCache.size >= PLACE_CACHE_MAX) placeCache.delete(placeCache.keys().next().value as string);
    placeCache.set(cacheKey, { at: Date.now(), items });
    return items;
  } catch (err) {
    console.warn('[geo] place 搜索失败:', (err as Error).message);
    return [];
  }
}

// ==================== 每用户搜索限流（10 次/分钟） ====================

const SEARCH_LIMIT = 10;
const searchRate = new Map<string, { minute: number; count: number }>();

export function assertSearchQuota(userId: string): void {
  const minute = Math.floor(Date.now() / 60000);
  const rec = searchRate.get(userId);
  if (!rec || rec.minute !== minute) {
    searchRate.set(userId, { minute, count: 1 });
    return;
  }
  rec.count += 1;
  if (rec.count > SEARCH_LIMIT) {
    throw new AppError(429, '搜索过于频繁，请稍后再试');
  }
}
```

`geo.ts` 顶部需要 `import { AppError } from '../utils/app-error.js';`。

- [ ] **Step 4: `src/routes/geo.routes.ts` 追加（先读该文件现状，沿用其 import/风格）**

```ts
// 地点关键词搜索（足迹编辑页自建选点用；额度敏感 → auth + 限流 + 服务端缓存）
fastify.get('/search', { onRequest: [fastify.authenticate] }, async (request) => {
  const { keyword, latitude, longitude } = request.query as {
    keyword?: string; latitude?: string; longitude?: string;
  };
  const kw = (keyword ?? '').trim();
  if (!kw) throw new AppError(400, '缺少搜索关键词 keyword');
  assertSearchQuota(request.user.userId);
  const lat = latitude != null ? Number(latitude) : undefined;
  const lng = longitude != null ? Number(longitude) : undefined;
  return success(await searchPlaces(kw, Number.isFinite(lat) ? lat : undefined, Number.isFinite(lng) ? lng : undefined));
});
```

（对应 import：`searchPlaces`、`assertSearchQuota`、`success`、`AppError` —— 按该文件已有 import 缺啥补啥。）

- [ ] **Step 5: 跑测试 + typecheck**

Run: `WX_MOCK_LOGIN=true node --import tsx --test --test-concurrency=1 test/geo-search.test.ts && npm run typecheck`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/services/geo.ts src/routes/geo.routes.ts test/geo-search.test.ts
git commit -m "feat: /geo/search 腾讯地点搜索代理（1h 缓存+每用户10次/分限流，测试注入点）"
```

---

### Task 4: 小程序「足迹」tab 骨架（app.json + 图标 + 主页壳）

**Files:**
- Modify: `sport_track_miniapp/miniprogram/app.json`
- Create: `miniprogram/assets/icons/tab-footprint-gray.png`、`tab-footprint-blue.png`
- Create: `miniprogram/pages/footprints/footprints.{js,wxml,wxss,json}`
- Create（占位可编译）: `miniprogram/packageFootRecords/pages/record-edit/record-edit.{js,wxml,wxss,json}`、`.../map-picker/map-picker.{js,wxml,wxss,json}`（Task 7/8 填充，本任务先建空页避免分包配置报错）

**Interfaces:**
- Produces: tab 页 `pages/footprints/footprints`（data 契约：`mode`、`records`(geo 缓存)、`items`(列表)、`popup.visible/record`）；`getApp().globalData.footprintsDirty` 刷新标志。
- Consumes: Task 2 后端接口（本地 dev 服务 3004，联调用 wechatide「不校验合法域名」）。

- [ ] **Step 1: 生成 tab 图标**

先量现有图标尺寸：`sips -g pixelWidth -g pixelHeight miniprogram/assets/icons/tab-home-gray.png`。用 ImageGen 生成 2 张同风格单色脚印图标（灰 `#999999` / 蓝 `#2b6cf6`、纯白底/透明底与现有图标一致——先 Read 一张现有 png 看是否透明底），再 `sips -z <h> <w> out.png` 归一尺寸，落到上述两个路径。风格不符则手绘简化路径（SVG 无法直接用于 tabBar，必须 PNG）。

- [ ] **Step 2: 改 `app.json`**

- `pages` 数组追加 `"pages/footprints/footprints"`（放数组末尾即可，首页仍是 `pages/index/index` 第 0 项）；
- `tabBar.list` 末尾追加：
```json
{
  "pagePath": "pages/footprints/footprints",
  "text": "足迹",
  "iconPath": "assets/icons/tab-footprint-gray.png",
  "selectedIconPath": "assets/icons/tab-footprint-blue.png"
}
```
- `requiredPrivateInfos` 追加 `"chooseLocation"`；
- `subpackages` 追加：
```json
{ "root": "packageFootRecords", "pages": ["pages/record-edit/record-edit", "pages/map-picker/map-picker"] }
```

- [ ] **Step 3: 写 tab 主页壳（地图/列表容器 + FAB，数据与交互 Task 5/6 填充）**

`footprints.js`：
```js
const api = require('../../services/api');

Page({
  data: {
    mode: 'map', // map | list
    loading: true,
    error: '',
    records: [], // /geo 轻量点缓存 {id,title,visitDate,latitude,longitude,coverPhoto}
    markers: [],
    items: [], page: 1, pageSize: 20, total: 0, hasMore: true, loadingList: false,
    popup: { visible: false, record: null },
  },
  onLoad() { this.loadAll(); },
  onShow() {
    const app = getApp();
    if (app.globalData.footprintsDirty) {
      app.globalData.footprintsDirty = false;
      this.loadAll();
    }
  },
  onPullDownRefresh() { this.loadAll().finally(() => wx.stopPullDownRefresh()); },
  async loadAll() {
    this.setData({ loading: true, error: '' });
    const seq = (this._seq = (this._seq || 0) + 1); // 请求序号守卫：只应用最后一次结果，防闪屏/竞态
    try {
      await Promise.all([this.loadGeo(seq), this.reloadList(seq)]);
      this.setData({ loading: false });
    } catch (e) {
      if (seq !== this._seq) return;
      this.setData({ loading: false, error: e.message || '加载失败' });
    }
  },
  loadGeo(seq) {
    return api.get('/footprint-records/geo').then((data) => {
      if (seq !== this._seq) return;
      this.setData({ records: data.items });
      this.buildMarkers();
    });
  },
  reloadList(seq) {
    this.setData({ items: [], page: 1, hasMore: true });
    return this.fetchPage(seq || this._seq);
  },
  fetchPage(seq) {
    this.setData({ loadingList: true });
    return api
      .get('/footprint-records', { page: this.data.page, pageSize: this.data.pageSize })
      .then((data) => {
        if (seq !== this._seq) return;
        const items = this.data.items.concat(data.items);
        this.setData({ items, total: data.total, hasMore: items.length < data.total, loadingList: false });
      })
      .catch((e) => { this.setData({ loadingList: false }); throw e; });
  },
  buildMarkers() { /* Task 5 实现 */ },
  switchMode(e) { this.setData({ mode: e.currentTarget.dataset.mode }); },
  openAdd() { wx.navigateTo({ url: '/packageFootRecords/pages/record-edit/record-edit' }); },
  noop() {},
});
```

`footprints.wxml`：
```xml
<view class="page">
  <view class="seg">
    <view class="seg-item {{mode === 'map' ? 'active' : ''}}" data-mode="map" bindtap="switchMode">地图</view>
    <view class="seg-item {{mode === 'list' ? 'active' : ''}}" data-mode="list" bindtap="switchMode">列表</view>
  </view>

  <view class="body">
    <view wx:if="{{loading}}" class="center-tip">加载中…</view>
    <view wx:elif="{{error}}" class="center-tip">{{error}} <text class="retry" bindtap="loadAll">重试</text></view>
    <block wx:else>
      <view wx:if="{{records.length === 0 && mode === 'map'}}" class="center-tip empty">还没有足迹，点右下角 ＋ 记录第一个去过的地方</view>
      <view wx:if="{{mode === 'map'}}" class="map-wrap"><!-- Task 5 挂 <map> --></view>
      <view wx:if="{{mode === 'list'}}" class="list-wrap"><!-- Task 6 列表 --></view>
    </block>
  </view>

  <!-- 详情弹窗 Task 5 -->
  <view class="fab" bindtap="openAdd">＋</view>
</view>
```

`footprints.json`：`{ "navigationBarTitleText": "足迹", "enablePullDownRefresh": true }`。
`footprints.wxss`：seg 切换条样式对齐项目既有 segmented（先 Grep `pages/tracks/tracks.wxss` 里的筛选 tab 样式做基准），fab 圆形蓝底 `#2b6cf6` 右下悬浮，body 高度 `calc(100vh - 分段条高)`。

- [ ] **Step 4: 编译验证**

wechatide MCP：`compile_wxml` 无错 → `simulator_open_page` 打开 `pages/footprints/footprints` → `simulator_screenshot` 确认 tab 项显示、空态与 FAB 正常。截图存证。

- [ ] **Step 5: Commit**

```bash
git add miniprogram/app.json miniprogram/assets/icons/tab-footprint-*.png miniprogram/pages/footprints miniprogram/packageFootRecords
git commit -m "feat: 足迹 tab 骨架——第4个tabBar+主包页壳+packageFootRecords 分包占位"
```

---

### Task 5: 地图 marker 聚合 + 详情弹窗

**Files:**
- Modify: `miniprogram/pages/footprints/footprints.{js,wxml,wxss}`（补 `buildMarkers`、map 标签、弹窗）

**Interfaces:**
- Consumes: `data.records`（Task 4）；`GET /footprint-records/:id`。
- Produces: `openPopup(record)`；marker `id` = `records` 数组下标 + 1。

- [ ] **Step 1: 查证聚合 API（写码前必做）**

WebFetch `https://developers.weixin.qq.com/miniprogram/dev/component/map.html`，确认三件事并按实际字段修正本任务代码：① `marker.joinCluster` 与 `<map clusters>` 的属性形状（cluster 是否必填 `iconPath`）；② 聚合事件名 `bindclusterclick` / `bindclusteritemclick` 及 `e.detail`（`clusterId`/`markerIds`/`markerId`）；③ 所需基础库版本（≥2.14.0）。**若开发者工具模拟器不渲染聚合**：真机预览验证；仍不可用则降级为组件内网格聚合（按 zoom 计算格子，>1 点合并为自绘数字气泡 marker，点击展开缩小一格），接口不动。降级与否在提交信息里注明。

- [ ] **Step 2: 地图模板与 markers**

`footprints.wxml` 的 `.map-wrap`：
```xml
<map
  class="fp-map"
  longitude="{{center.longitude}}" latitude="{{center.latitude}}" scale="{{scale}}"
  markers="{{markers}}" clusters="{{clusters}}"
  include-points="{{includePoints}}"
  show-location
  bindmarkertap="onMarkerTap"
  bindclusteritemclick="onClusterItemClick"
  bindpoitap="onPoiTap"
/>
```

`buildMarkers()` 与视野：
```js
buildMarkers() {
  const records = this.data.records;
  const markers = records.map((r, i) => ({
    id: i + 1,                 // 原生聚合要求数字 id
    latitude: r.latitude,
    longitude: r.longitude,
    joinCluster: true,
    title: r.title,            // 部分端上聚合展开列表用 title
  }));
  const points = records.map((r) => ({ latitude: r.latitude, longitude: r.longitude }));
  const patch = { markers };
  if (points.length === 1) {
    // 教训：单点 includePoints 会把 scale 顶到 20 —— 改手动定位
    patch.includePoints = [];
    patch.center = { latitude: points[0].latitude, longitude: points[0].longitude };
    patch.scale = 12;
  } else if (points.length > 1) {
    patch.includePoints = [{ padding: [80, 60, 160, 60], points }]; // 视野由 includePoints 决定，不动 scale
  }
  this.setData(patch);
},
```
（`include-points` 的项结构为 `{padding:[t,r,b,l], points:[{latitude,longitude}]}`，以官方文档为准；`data` 里补 `center:{latitude:30.5,longitude:114.3}, scale:12, includePoints:[], clusters:[{id:0, color:'#ffffff', bgcolor:'#2b6cf6', size:40}]`。）

```js
onMarkerTap(e) {
  const r = this.data.records[Number(e.detail.markerId) - 1];
  if (r) this.openPopup(r);
},
onClusterItemClick(e) {
  const r = this.data.records[Number(e.detail.markerId) - 1];
  if (r) this.openPopup(r);
},
onPoiTap(e) {
  const r = this.data.records.find((x) => x.latitude === e.detail.latitude && x.longitude === e.detail.longitude);
  if (r) this.openPopup(r);
},
```

- [ ] **Step 3: 详情弹窗（点 marker/列表卡共用）**

```js
openPopup(record) {
  // geo/列表轻量数据缺描述与人物时补拉详情
  const need = record.description === undefined || record.people === undefined;
  const fill = need ? api.get(`/footprint-records/${record.id}`) : Promise.resolve(record);
  fill.then((full) => this.setData({ popup: { visible: true, record: full } }))
      .catch((e) => wx.showToast({ title: e.message || '加载失败', icon: 'none' }));
},
closePopup() { this.setData({ 'popup.visible': false }); },
editRecord() {
  const id = this.data.popup.record.id;
  this.setData({ 'popup.visible': false });
  wx.navigateTo({ url: `/packageFootRecords/pages/record-edit/record-edit?id=${id}` });
},
removeRecord() {
  const { id, title } = this.data.popup.record;
  wx.showModal({
    title: '删除足迹', content: `确定删除「${title}」？照片将一并移除`, confirmColor: '#e54d42',
    success: (res) => {
      if (!res.confirm) return;
      api.del(`/footprint-records/${id}`).then(() => {
        this.setData({ 'popup.visible': false });
        wx.showToast({ title: '已删除', icon: 'success' });
        this.loadAll();
      }).catch((e) => wx.showToast({ title: e.message, icon: 'none' }));
    },
  });
},
```

wxml（`.page` 尾部、fab 之前）：
```xml
<view wx:if="{{popup.visible}}" class="mask" bindtap="closePopup">
  <view class="sheet" catchtap="noop">
    <view class="sheet-title">{{popup.record.title}}</view>
    <view class="sheet-meta">{{popup.record.visitDate}} · {{popup.record.location.city || popup.record.location.address}}</view>
    <view wx:if="{{popup.record.people.length}}" class="sheet-people">同行：{{popup.record.people}}</view>
    <view wx:if="{{popup.record.description}}" class="sheet-desc">{{popup.record.description}}</view>
    <scroll-view wx:if="{{popup.record.photos.length}}" scroll-x class="sheet-photos">
      <image wx:for="{{popup.record.photos}}" wx:key="*this" src="{{item}}" mode="aspectFill" class="sheet-photo" data-urls="{{popup.record.photos}}" data-idx="{{index}}" bindtap="previewPhoto"/>
    </scroll-view>
    <view class="sheet-actions">
      <button class="act-btn" size="mini" bindtap="editRecord">编辑</button>
      <button class="act-btn danger" size="mini" bindtap="removeRecord">删除</button>
    </view>
  </view>
</view>
```
wxss：mask 全屏半透明黑、sheet 底部白色圆角浮层（对齐项目弹层惯例——参照 report 周期选择弹窗观感）。`previewPhoto(e)` 用 `wx.previewImage({urls, current: urls[idx]})`。列表数据 `items[i]` 是完整 DTO，走 `openPopup` 时 `need===false` 直接展示。

- [ ] **Step 4: 模拟器验证**

后端起 dev（3004）。wechatide：先经编辑页/接口造 5+ 条同城 + 2 条异地数据（可用 `automation_evaluate` 直接 fetch 后端灌数据），切到足迹 tab 截图验证：聚合簇出现、点单点/簇内点弹窗、编辑/删除按钮、图片预览。聚合表现记入 `docs/11-足迹功能设计.md` §4.2 的验证结论（模拟器 or 真机）。

- [ ] **Step 5: Commit**

```bash
git add miniprogram/pages/footprints
git commit -m "feat: 足迹地图原生 marker 聚合+详情弹窗（含单点 includePoints 防顶满 scale）"
```

---

### Task 6: 列表形态（分页卡片）

**Files:**
- Modify: `miniprogram/pages/footprints/footprints.{js,wxml,wxss}`

- [ ] **Step 1: 列表模板（替换 Task 4 的 `.list-wrap` 占位）**

```xml
<scroll-view scroll-y class="list-scroll" bindscrolltolower="onListReachBottom">
  <view wx:for="{{items}}" wx:key="id" class="fp-card" data-idx="{{index}}" bindtap="onCardTap">
    <image wx:if="{{item.photos.length}}" src="{{item.photos[0]}}" mode="aspectFill" class="fp-thumb"/>
    <view wx:else class="fp-thumb placeholder">图</view>
    <view class="fp-card-main">
      <view class="fp-card-title">{{item.title}}</view>
      <view class="fp-card-sub">{{item.visitDate}} · {{item.location.city || item.location.address || '未知地点'}}</view>
      <view wx:if="{{item.people.length}}" class="fp-card-people">{{item.people}}</view>
    </view>
  </view>
  <view wx:if="{{loadingList}}" class="list-tip">加载中…</view>
  <view wx:elif="{{!hasMore && items.length}}" class="list-tip">没有更多了</view>
  <view wx:if="{{items.length === 0}}" class="center-tip empty">还没有足迹，点右下角 ＋ 记录第一个去过的地方</view>
</scroll-view>
```

```js
onListReachBottom() {
  if (!this.data.hasMore || this.data.loadingList) return;
  this.setData({ page: this.data.page + 1 });
  this.fetchPage(this._seq);
},
onCardTap(e) {
  const r = this.data.items[Number(e.currentTarget.dataset.idx)];
  if (r) this.openPopup(r); // items 是完整 DTO：弹窗不再二次请求
},
```
卡片样式基准：Grep `pages/tracks/tracks.wxss` 的列表卡片（圆角白卡/缩略图尺寸/次要文字色），保持两页一致。

- [ ] **Step 2: 模拟器验证**

列表首屏 20 条、触底加载、切换 map⇄list 不闪屏（loading 态只在首次全量出现）、卡片点击弹窗。截图。

- [ ] **Step 3: Commit**

```bash
git add miniprogram/pages/footprints
git commit -m "feat: 足迹列表形态——分页卡片+触底加载，样式对齐轨迹列表卡片"
```

---

### Task 7: 编辑页（表单 + chooseLocation + 压缩上传 + 删除入口）

**Files:**
- Modify/Create: `miniprogram/packageFootRecords/pages/record-edit/record-edit.{js,wxml,wxss,json}`
- Modify: `miniprogram/app.js`（`globalData` 增 `footprintsDirty: false`，若无 globalData 则补）

**Interfaces:**
- Consumes: `services/api` 的 get/post/put/del；`services/oss-upload.js` 的 `uploadPhoto(path,{dir:'footprints',prefix:'fp_'})`（内部含 checkImage 合规，`{blocked}`/`null` 需处理）；Task 8 map-picker 经 eventChannel 回传（本任务先留空函数）。
- Produces: 保存成功后 `getApp().globalData.footprintsDirty = true`。

- [ ] **Step 1: 页面逻辑（核心代码）**

```js
const api = require('../../../services/api');
const { uploadPhoto } = require('../../../services/oss-upload');
const MAX_PHOTOS = 3;

Page({
  data: {
    id: '', visitDate: '', title: '', people: [], peopleInput: '',
    description: '', location: null, // {name,address,latitude,longitude}
    photos: [],        // [{ url: 已上传裸/签名URL, localPath: 临时文件|null }]
    submitting: false,
    canSubmit: false,
  },
  onLoad(q) {
    this.setData({ visitDate: this.today() });
    if (q && q.id) {
      this.setData({ id: q.id });
      wx.setNavigationBarTitle({ title: '编辑足迹' });
      api.get(`/footprint-records/${q.id}`).then((r) => {
        this.setData({
          visitDate: r.visitDate, title: r.title, people: r.people,
          description: r.description, location: r.location,
          photos: (r.photos || []).map((url) => ({ url, localPath: null })),
        });
        this.refreshCanSubmit();
      });
    }
  },
  today() {
    const d = new Date();
    const p = (n) => String(n).padStart(2, '0');
    return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}`;
  },
  onDate(e) { this.setData({ visitDate: e.detail.value }); this.refreshCanSubmit(); },
  onTitle(e) { this.setData({ title: e.detail.value }); this.refreshCanSubmit(); },
  onDesc(e) { this.setData({ description: e.detail.value }); },
  onPeopleInput(e) { this.setData({ peopleInput: e.detail.value }); },
  addPerson() {
    const name = this.data.peopleInput.trim();
    if (!name) return;
    if (this.data.people.length >= 10) return wx.showToast({ title: '人物最多 10 个', icon: 'none' });
    if (this.data.people.includes(name)) return this.setData({ peopleInput: '' });
    this.setData({ people: this.data.people.concat(name), peopleInput: '' });
  },
  removePerson(e) {
    const i = Number(e.currentTarget.dataset.idx);
    this.setData({ people: this.data.people.filter((_, j) => j !== i) });
  },

  /** 主选点：wx.chooseLocation（免费，自带搜索+地图点选） */
  pickByWx() {
    wx.chooseLocation({
      success: (res) => {
        this.setData({ location: { name: res.name || res.address, address: res.address, latitude: res.latitude, longitude: res.longitude } });
        this.refreshCanSubmit();
      },
      fail: (e) => { if (String(e.errMsg).includes('auth deny')) wx.showToast({ title: '需要位置权限才能选点', icon: 'none' }); },
    });
  },
  /** 备用：自建地图选点页（Task 8 实现其页面） */
  pickBySelfMap() {
    wx.navigateTo({
      url: '/packageFootRecords/pages/map-picker/map-picker',
      events: {
        acceptPick: (loc) => { this.setData({ location: loc }); this.refreshCanSubmit(); },
      },
    });
  },

  refreshCanSubmit() {
    const d = this.data;
    this.setData({ canSubmit: Boolean(d.visitDate && d.title.trim() && d.location) });
  },

  /** 选图→压缩→（上传在提交时做，避免浪费 OSS 流量） */
  choosePhoto() {
    const remaining = MAX_PHOTOS - this.data.photos.length;
    if (remaining <= 0) return wx.showToast({ title: `最多 ${MAX_PHOTOS} 张`, icon: 'none' });
    wx.chooseMedia({
      count: remaining, mediaType: ['image'], sourceType: ['camera', 'album'], sizeType: ['compressed'],
      success: (res) => {
        const picked = res.tempFiles.map((f) => f.tempFilePath).filter((p) => !this.data.photos.some((x) => x.localPath === p));
        this.setData({ photos: this.data.photos.concat(picked.map((p) => ({ url: '', localPath: p }))).slice(0, MAX_PHOTOS) });
      },
    });
  },
  removePhoto(e) {
    const i = Number(e.currentTarget.dataset.idx);
    this.setData({ photos: this.data.photos.filter((_, j) => j !== i) });
  },
  compress(path) {
    return new Promise((resolve) => {
      wx.compressImage({ src: path, quality: 80, success: (r) => resolve(r.tempFilePath), fail: () => resolve(path) });
    });
  },
  async uploadPending() {
    const out = [];
    for (const p of this.data.photos) {
      if (!p.localPath) { out.push(p.url); continue; } // 已有图：保留（服务端 cleanUrl 归一签名）
      const compressed = await this.compress(p.localPath);
      const r = await uploadPhoto(compressed, { dir: 'footprints', prefix: 'fp_' });
      if (!r) throw new Error('照片上传失败，请重试');
      if (r.blocked) { wx.showToast({ title: '照片含违规内容已移除', icon: 'none' }); continue; }
      out.push(r.url);
    }
    return out;
  },

  async submit() {
    if (!this.data.canSubmit || this.data.submitting) return;
    this.setData({ submitting: true });
    try {
      const photos = await this.uploadPending();
      const payload = {
        visitDate: this.data.visitDate, title: this.data.title.trim(),
        people: this.data.people, description: this.data.description.trim(),
        location: { name: this.data.location.name, address: this.data.location.address, latitude: this.data.location.latitude, longitude: this.data.location.longitude },
        photos,
      };
      if (this.data.id) await api.put(`/footprint-records/${this.data.id}`, payload);
      else await api.post('/footprint-records', payload);
      getApp().globalData.footprintsDirty = true;
      wx.showToast({ title: '已保存', icon: 'success' });
      setTimeout(() => wx.navigateBack(), 600);
    } catch (e) {
      wx.showToast({ title: e.message || '保存失败', icon: 'none' });
      this.setData({ submitting: false });
    }
  },
});
```

- [ ] **Step 2: 表单 WXML/WXSS**

字段顺序=地点（两个入口按钮：`地图选点`→pickBySelfMap、`快速选点`→pickByWx；已选显示 name/address+重选）→ 日期 `picker mode="date"` → 标题 input → 人物（input+添加按钮，标签可点删）→ 描述 textarea maxlength=500 → 图片九宫格（+占位、x 删除）→ 底部提交 button（disabled=`!canSubmit || submitting`）。样式对齐 profile/settings 页表单卡片风格。json：`{"navigationBarTitleText":"新增足迹"}`。

- [ ] **Step 3: 模拟器全链路验证**

Tab→FAB→填表→chooseLocation 选点（模拟器会弹地图，`automation_evaluate`/手动均可）→选 3 图→保存→回 tab 看到新点与新卡片；编辑（弹窗→改标题→删 1 图→保存）；删除（弹窗→确认→点消失）。检查 network 里 compress→credential→oss PUT 序列与 `/footprint-records` 200。截图存证。

- [ ] **Step 4: Commit**

```bash
git add miniprogram/packageFootRecords miniprogram/app.js
git commit -m "feat: 足迹编辑页——chooseLocation 选点/压缩直传 OSS/保存回刷 tab"
```

---

### Task 8: 自建地图选点页 `map-picker`

**Files:**
- Create: `miniprogram/packageFootRecords/pages/map-picker/map-picker.{js,wxml,wxss,json}`

- [ ] **Step 1: 页面逻辑**

```js
const api = require('../../../services/api');

Page({
  data: {
    center: { latitude: 30.5, longitude: 114.3 }, scale: 12,
    picked: null, // {latitude, longitude, name, address}
    keyword: '', results: [], searching: false, searchError: '',
  },
  onLoad() {
    // 以当前位置为初始视野（拒绝授权则用默认中心，不阻塞）
    wx.getLocation({
      type: 'gcj02',
      success: (r) => this.setData({ center: { latitude: r.latitude, longitude: r.longitude } }),
    });
  },
  onKeyword(e) {
    this.setData({ keyword: e.detail.value });
    clearTimeout(this._t);
    const kw = e.detail.value.trim();
    if (!kw) return this.setData({ results: [] });
    this._t = setTimeout(() => this.doSearch(kw), 500); // 防抖 500ms 省额度
  },
  async doSearch(kw) {
    this.setData({ searching: true, searchError: '' });
    try {
      const { latitude, longitude } = this.data.center;
      const items = await api.get('/geo/search', { keyword: kw, latitude, longitude });
      this.setData({ results: items, searching: false });
    } catch (e) {
      // 限流/上游失败不阻塞点选主路径
      this.setData({ searching: false, searchError: e.message || '搜索失败，可直接点选地图' });
    }
  },
  pickResult(e) {
    const p = this.data.results[Number(e.currentTarget.dataset.idx)];
    this.setData({ picked: { latitude: p.latitude, longitude: p.longitude, name: p.name, address: p.address }, center: { latitude: p.latitude, longitude: p.longitude } });
  },
  /** 直接点地图：坐标 → /geo/reverse 回填地址 */
  onMapTap(e) {
    const { latitude, longitude } = e.detail;
    if (typeof latitude !== 'number') return;
    this.setData({ picked: { latitude, longitude, name: '', address: '解析中…' } });
    api.get('/geo/reverse', { latitude, longitude })
      .then((data) => this.setData({ 'picked.address': data.address || '', 'picked.name': data.address || '' }))
      .catch(() => this.setData({ 'picked.address': '', 'picked.name': '地图选点' }));
  },
  confirm() {
    if (!this.data.picked) return wx.showToast({ title: '请先选择地点', icon: 'none' });
    this.getOpenerEventChannel().emit('acceptPick', this.data.picked);
    wx.navigateBack();
  },
});
```

（`/geo/reverse` 返回体以 `src/routes/geo.routes.ts` 现状为准——实现时先 curl dev 3004 确认字段名再对上。）

- [ ] **Step 2: 模板**：顶部搜索框 + 结果下拉列表（name 主行/address 次行）→ 全屏 `<map latitude longitude scale show-location bindtap="onMapTap"/>` → 底部浮层显示 `picked` 摘要 + 「确认选点」按钮。搜索失败横幅内嵌 `searchError`，不打断点选。
- [ ] **Step 3: 模拟器验证**：编辑页→「地图选点」→搜"火车站"出结果、点结果图钉移动、空白处 `automation_evaluate` 触发 onMapTap 或工具点击→地址回填→确认→编辑页表单已填。截图。
- [ ] **Step 4: Commit**

```bash
git add miniprogram/packageFootRecords/pages/map-picker
git commit -m "feat: 自建地图选点页——/geo/search 防抖搜索+点选逆地理回填+eventChannel 回传"
```

---

### Task 9: 全链路回归 + 收尾

- [ ] **Step 1: API 回归**：`cd sport_track_api && npm test && npm run typecheck`，全绿。
- [ ] **Step 2: 小程序回归矩阵**（wechatide 截图存证）：未登录进 tab（应提示而非白屏）；1 条记录（不出现聚合、scale 正常）；30+ 条密集（簇出现、点击展开、弹窗）；图片 3 张超限拦截；编辑保留原图不重复上传（network 断言：无新增 credential 请求）。
- [ ] **Step 3: 文档回填**：把聚合验证结论（原生 or 降级、模拟器 or 真机）写回 `docs/11-足迹功能设计.md` §4.2；本文档勾掉已完成 checkbox。
- [ ] **Step 4: 各自 commit，停在"等用户真机测试"**（不 push，跨仓库分开提交）。

## Self-Review 结论

- Spec 覆盖：§2 模型=Task 1；§3 接口=Task 2/3；§4.1 tab/分包=Task 4；§4.2 地图+弹窗=Task 5、列表=Task 6；§4.3 编辑/选点=Task 7/8；§5 安全（限流/归属/cleanUrl）内嵌各任务测试；§6 验证=Task 4~9。无缺口。
- 类型一致性：`FootprintDto.photos` 为签名 URL、geo 的 `coverPhoto`、marker id = 下标+1、`fp_` 前缀、`dir:'footprints'` 三处引用一致。
- 已知风险：①聚合 API 细节以 Step 1 查证为准（有降级路径）；②`/geo/reverse` 返回字段实现时需 curl 确认。
